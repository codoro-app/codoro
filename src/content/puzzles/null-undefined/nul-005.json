{
  "id": "nul-005",
  "pattern": "null-undefined",
  "difficulty_rating": 1575,
  "explanation": "Destructuring defaults like `name = 'Guest'` only activate when the property is `undefined` — they do not treat `null` as missing. The call passes `name: null` explicitly, so the default never fires and the template literal renders `\"null#42\"` instead of the intended `\"Guest#42\"`.",
  "prompt": "Does this function safely fall back to 'Guest' whenever no name is provided?",
  "language": "javascript",
  "snippet": "function formatUsername({ name = 'Guest', id } = {}) {\n  return `${name}#${id}`;\n}\n\nconsole.log(formatUsername({ name: null, id: 42 }));",
  "interaction": "swipe-binary",
  "left_label": "Bug: null bypasses default",
  "right_label": "Safe",
  "correct_direction": "left",
  "correct_verdict": "bug"
}
