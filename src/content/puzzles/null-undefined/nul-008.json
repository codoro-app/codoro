{
  "id": "nul-008",
  "pattern": "null-undefined",
  "difficulty_rating": 1050,
  "explanation": "The function assumes `user` is always a valid object and immediately accesses `user.email` with no null/undefined check. If `user` is `null` or `undefined` (e.g., a lookup that found no match), this throws a TypeError before `.toLowerCase()` ever runs.",
  "prompt": "Swipe: is this function safe to call, or does it crash on certain inputs?",
  "language": "javascript",
  "snippet": "function getUserEmail(user) {\n  return user.email.toLowerCase();\n}",
  "interaction": "swipe-binary",
  "left_label": "Crashes on null",
  "right_label": "Safe",
  "correct_direction": "left",
  "correct_verdict": "bug"
}
