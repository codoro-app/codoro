{
  "id": "cf-004",
  "pattern": "control-flow",
  "difficulty_rating": 1575,
  "explanation": "The `case 'gold':` branch is missing a `break`, so execution falls through into `case 'silver':` and overwrites `discount` with 0.1 before hitting the first `break`. A gold-tier customer ends up with the silver discount instead of the gold one, even though every other case behaves as expected.",
  "prompt": "Does this discount switch behave correctly for every tier?",
  "language": "javascript",
  "snippet": "function getDiscount(tier) {\n  let discount = 0\n  switch (tier) {\n    case 'gold':\n      discount = 0.2\n    case 'silver':\n      discount = 0.1\n      break\n    default:\n      discount = 0\n  }\n  return discount\n}",
  "interaction": "swipe-binary",
  "left_label": "Fall-through bug",
  "right_label": "Correct",
  "correct_direction": "left"
}
