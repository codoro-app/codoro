{
  "id": "res-011",
  "pattern": "resource-management",
  "difficulty_rating": 1475,
  "explanation": "The two readLine() calls print the two lines of the file in order — 'first' then 'second' — while the reader is still open. try-with-resources then runs close() automatically as the block exits, and only after that does the statement following the block print 'closed'. So the reader is released before 'closed' appears.",
  "prompt": "Drag these steps into the order they happen for a two-line file.",
  "language": "java",
  "snippet": "try (BufferedReader r = Files.newBufferedReader(path)) {\n    System.out.println(r.readLine());\n    System.out.println(r.readLine());\n}\nSystem.out.println(\"closed\");",
  "interaction": "drag-order",
  "format": "output",
  "blocks": ["Prints 'closed'", "Prints 'second'", "Auto-closes the reader", "Prints 'first'"],
  "correct_order": [3, 1, 2, 0]
}
