{
  "id": "err-005",
  "pattern": "error-handling",
  "difficulty_rating": 1075,
  "explanation": "The catch block is empty, so when `JSON.parse` throws on invalid input, the error is silently swallowed and the function returns `undefined` instead of surfacing the failure. Callers have no way to know parsing failed, which hides bugs and makes debugging much harder.",
  "prompt": "Is this config parser's error handling okay?",
  "language": "javascript",
  "snippet": "function parseConfig(json) {\n  try {\n    return JSON.parse(json)\n  } catch (e) {\n  }\n}",
  "interaction": "swipe-binary",
  "left_label": "Swallows the error",
  "right_label": "Safe",
  "correct_direction": "left",
  "correct_verdict": "bug"
}
