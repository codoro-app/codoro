{
  "id": "rec-009",
  "pattern": "recursion-termination",
  "difficulty_rating": 1200,
  "explanation": "Recursive calls fully unwind before any multiplication happens: factorial(3) can't compute its own result until factorial(2) returns, which can't compute its result until factorial(1) hits the base case. So the calls happen top-down (3 calls 2, 2 calls 1), but the returns happen bottom-up (1 returns first, then 2's multiplication, then 3's) — the reverse order of the calls.",
  "prompt": "Drag these steps into the order they actually happen when factorial(3) runs.",
  "language": "javascript",
  "snippet": "function factorial(n) {\n  if (n <= 1) return 1;\n  return n * factorial(n - 1);\n}\nfactorial(3);",
  "interaction": "drag-order",
  "format": "output",
  "blocks": [
    "factorial(2) returns 2 * 1 = 2",
    "factorial(1) hits the base case and returns 1",
    "factorial(3) calls factorial(2)",
    "factorial(3) returns 3 * 2 = 6",
    "factorial(2) calls factorial(1)"
  ],
  "correct_order": [2, 4, 1, 0, 3]
}
