# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
!.env.example

# Worker secrets for `wrangler dev` (workers/) — real secrets are
# `wrangler secret put`, never a committed file
.dev.vars
!.dev.vars.example

# Build info / coverage
*.tsbuildinfo
coverage

# wrangler's local dev state (pnpm dev:pages) — machine-local, not source
.wrangler/

# Claude Code local state
.claude/

todo.md

# Superpowers skill workspaces (SDD ledgers/briefs, brainstorm sessions) — scratch, not source
.superpowers/