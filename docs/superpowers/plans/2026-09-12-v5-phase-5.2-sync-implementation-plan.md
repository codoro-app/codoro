# v5 Phase 5.2 — sync: detailed implementation plan (2026-09-12)

Companion to `docs/superpowers/plans/2026-08-27-v5-accounts-implementation-plan.md` (T6/T7/T8 — this is that plan's Phase 5.2 section expanded to task-by-task detail, in the same convention as the 5.0 coding plan and the 5.1 build prompt) and `docs/v5-build-plan.md`'s Phase 5.2 section (the DoDs this plan is scoped to satisfy).

**Read first, every session:** I1–I10 in the accounts implementation plan, the Footgun Register (F1–F23), and this document's own footgun additions below (F24–F28). A task that violates an invariant is wrong even if its tests pass.

## Phase 5.1 status — verified before writing this plan (not assumed)

Checked against the repo directly (git log, source tree, workers routes) rather than taken from the plan doc's own claims:

- **T5 merged to `main`** (#126), then hardened by seven real-world fix PRs (#127–#133) once real Clerk keys existed and Thomas ran the actual flow against production via Claude in Chrome: a dead-CSS-preload crash on live `/settings` (#128, critical), missing captcha mount point (#129), password minLength mismatch with Clerk's production policy (#130), a verification email that was never actually being sent (#131), a stale Worker route blocking `deploy-dev` CI (#132), and username collection + real Clerk error surfacing (#133, `missing_requirements` deadlock). One more commit (`sign-up's username also fills in the challenge name`) is pushed to `feat/signup-sets-challenger-name` but **not yet merged to `main`** — small (29 lines, one file), not a blocker for 5.2 planning, worth landing first so 5.2 starts from a clean `main`.
- **DoD status, from the T5 session amendment plus the fix-PR trail:**
  - Signed-out play loop unchanged, bundle diff + Lighthouse recorded — **done**.
  - Create → sign out → sign in → delete round-trip — server-side deletion (D1 row gone, idempotent, cross-user isolated) was verified against local bindings in T5 itself; the _client_ round-trip is what #127–#133 exist to make actually work end-to-end against Clerk Production (email verification literally didn't fire before #131) — **functionally done now**, but no single amendment explicitly re-states "the full round-trip was clicked through end-to-end post-fixes." Worth a five-minute manual pass before calling 5.1 fully closed, not a reason to hold 5.2.
  - Signup prompts: frequency-cap logic is unit-tested (15 tests) and one of four triggers is wired end-to-end (`stats-second-visit`). **`boss-clear` and `streak-7-day` are built (copy + config) but not wired to real UI** — a named, scoped gap in the T5 amendment, not an oversight discovered now. `leaderboard-view` structurally can't be wired before 5.3 (no leaderboard page exists).
  - Report control: signed-out, fire-and-forget, server round-trip tested; a manual click-through against `codoro-dev` was noted as "not yet performed" in the T5 amendment — likely covered incidentally by the live debugging in #128 (which used `getcodoro.com/settings` and Claude in Chrome), not confirmed explicitly.
  - `docs/v5-build-plan.md`'s own Phase 5.1 DoD checklist is still all `- [ ]` — nobody has written the 5.1 closing amendment (the same housekeeping step #124 did for 5.0). **Recommend doing that now**, before or alongside 5.2's Piece 0, so the record matches reality.
- **Net assessment: Phase 5.1 is substantively complete.** The core client-auth surface (sign-up, sign-in, delete, settings) is live in production and has been debugged against real usage, which is a stronger bar than the original DoD's staging-only ask. The two real gaps — two of four signup-prompt triggers unwired, and no written 5.1 closing amendment — are small, named, and don't block 5.2 (sync doesn't depend on either). Recommend closing them in Piece 0 below rather than letting them sit as an ambiguous "done or not" question.

## What already exists that 5.2 builds on (verified in the repo, not assumed from the plan)

- **D1 schema is already fully built** — migration `0001_init.sql` (landed in Phase 5.0, T2/T2a) already contains `profiles` (compressed `BLOB payload` + `payload_bytes`, per the 2026-08-31 amendment's S1) with `revision`, `schema_version`, `updated_at`. `workers/src/profileStore.ts` (T2a's boundary) already exists with the get/put/delete surface the amendment describes, gzip in the Worker via `CompressionStream`/`DecompressionStream`. **T7 does not create this table or the compression boundary — both are done.** T7 is: write the two route handlers, the optimistic-concurrency check, and the payload-cap validation on top of `profileStore.ts`.
- **`scores`/`scores_best` tables also already exist** (S3, plus migration `0002` for an ops index) — built ahead of schedule in Phase 5.0 rather than in T10/Phase 5.3 as the original plan sequenced it. Not this phase's problem, but worth knowing so nobody re-derives the DDL.
- **`workers/src/index.ts` currently registers exactly three routes**: `GET /api/health`, `POST /api/report`, `DELETE /api/account`. No `/api/profile`, no `/api/scores`, no `/api/leaderboard`, no `/api/username`, no `/api/privacy` — **confirms 5.2 has not been started**.
- **`src/auth/api.ts`** is the one fetch wrapper (T5) — `apiFetch(path, { method, token, body, timeoutMs })`, throws typed `ApiError` with a `kind` taxonomy (`network`/`timeout`/`unauthorized`/`forbidden`/`not-found`/`rate-limited`/`server`/`invalid-response`/`client`). **T8's sync engine calls this, doesn't reinvent it.**
- **`src/storage/`** is the existing local-first layer T6/T8 must not couple to structurally: `schema.ts` (`UserProfile` interface, `CURRENT_SCHEMA_VERSION = 13`), `profile.ts` (`loadProfile()`/`saveProfile(profile)` — the single write boundary, called directly by feature code like `BossPage.tsx`, no pub/sub layer exists), `attempts.ts` (`appendAttempt()`/`listAttempts()` — attempts are a **separate IndexedDB store**, not a field on `UserProfile`), `migrations.ts` (`MIGRATIONS`/`runMigrations()` — the forward-only chain T6's `blob < CURRENT` branch reuses), `exportImport.ts` (`ExportedData = { schema_version, exportedAt, profile: UserProfile, attempts: Attempt[] }` — the wire shape sync pushes/pulls; note `importData()` is a **wholesale replace** of both stores, not a merge — T6 is new logic, not an extension of anything here).
- **No pub/sub/listener layer exists in `src/storage/`** (verified — no `addEventListener`/`subscribe`/`onChange` pattern). The plan's "flows through the normal storage listeners" line doesn't correspond to a real mechanism yet in this codebase; T8 has to decide how push-after-boundary actually triggers (see the open design question in T8 below — this is new, not a wiring exercise).
- **`fast-check` is already a devDependency** (`^4.9.0`) — T6's property tests use it directly, no new dependency to add.
- **No real pre-v5 export fixture exists in the repo yet** (`find . -iname "*fixture*"` — nothing). T8's DoD explicitly requires replaying migration against "a real pre-v5 export file," not a synthetic one — getting one is a Piece 0 prep step, not a T8-time scramble.
- **`UserProfile`'s actual fields** (for the merge-rule table, from `src/storage/schema.ts`): `schema_version, rating, ratedAttemptCount, streak, requeueState, storagePersisted, dailyCompletion, rushStats, bestRunStreak, bossStats, missionProgress, missionStats, preferences, anonId, challengerName, firstRunCompleted`. Plus `attempts: Attempt[]` as a sibling in `ExportedData`, not a `UserProfile` field. This is the exhaustive list T6's `Record<keyof Profile, MergeRule>` must cover.

## Piece 0 — close out 5.1's loose ends (small, do first)

Not because 5.2 depends on it technically, but because starting Phase 5.2 while Phase 5.1's own record is ambiguous is how "is 5.1 done?" becomes a recurring question instead of a closed one.

1. Merge `feat/signup-sets-challenger-name` (or finish whatever review it's waiting on).
2. Manual pass: create → sign out → sign in → delete, end to end against `getcodoro.com`, post-#133. Takes five minutes: it's the one 5.1 DoD line with no explicit "verified post-fix" evidence.
3. Write the Phase 5.1 closing amendment at the bottom of the implementation plan doc, same convention as T1–T4a's and T5's own: what shipped, the fix-PR trail as the real-world verification evidence, `boss-clear`/`streak-7-day` named as deliberately-deferred scoped follow-up (not silently dropped), report-control click-through status. Check off `docs/v5-build-plan.md`'s Phase 5.1 DoD boxes against real evidence, same as #124 did for 5.0.
4. Decide, explicitly, whether `boss-clear`/`streak-7-day` wiring happens now (cheap — `SignupPromptTrigger` is already generic, per the T5 amendment) or is deliberately deferred past 5.2. Either is fine; leaving it undecided isn't.

None of this blocks starting T6 in parallel if you'd rather — T6 is pure logic with zero dependency on 5.1's UI surface — but it should land before 5.2's own closing amendment gets written, so that one doesn't inherit 5.1's unfinished bookkeeping.

## New invariants for this phase

| #   | Invariant                                                                                                                                                                                                                                  | Concrete test                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I11 | **Merge never runs twice for the same pulled revision.** Applying the same server blob to the same local state a second time (a retried pull, a duplicate event) must be a no-op, not a double-union or a re-recompute drift               | `merge(merge(local, remote), remote) === merge(local, remote)` — idempotence test alongside T6's existing commutativity/associativity-style property tests |
| I12 | **A push never races a pull for the same local write.** The engine has exactly one in-flight direction at a time per boundary; a pull arriving mid-push (or vice versa) queues, it doesn't interleave against a half-written `UserProfile` | Concurrency test: trigger pull and push in the same tick, assert `saveProfile` is called with a fully-merged, never a partial, state                       |

These sit alongside I1–I10 unchanged — I2 (local-first lock) and I8 (SW never caches `/api/*`) are the two that bite hardest in this phase specifically, since T8 is the first task that makes real network calls on every normal play session rather than only at auth boundaries.

## T6 — merge engine, pure and alone (1 session — no I/O, no network)

**Files:** `src/sync/merge.ts`, `src/sync/merge.test.ts`, `src/sync/fixtures/` (real export files, see Piece 0-adjacent prep below).

Per-field merge rule, against the actual `UserProfile` shape verified above (write this table into the module's own doc comment, exhaustive via `Record<keyof UserProfile, MergeRule>` so a new profile field with no rule fails typecheck, not silently falls through):

| Field                                                                                                         | Rule                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `schema_version`                                                                                              | not merged directly — governs which branch of the skew table (below) runs; post-merge the result is stamped with the client's own `CURRENT_SCHEMA_VERSION`                                                                                           |
| `rating`                                                                                                      | **recompute**: re-derive from the merged `attempts` array via the same rating engine the client already uses for a fresh attempt, not a max/LWW pick between the two raw numbers                                                                     |
| `ratedAttemptCount`                                                                                           | **recompute**, alongside `rating` — same source, same pass                                                                                                                                                                                           |
| `streak`                                                                                                      | **recompute** from merged attempts' `localDateString`s, per the existing streak-computation logic — not max, not LWW (a stale streak can't just be the bigger number; it has to be re-derived from the actual merged activity)                       |
| `requeueState`                                                                                                | **latest `clientUpdatedAt` wins** — it's session-local scheduling state, not accumulated history                                                                                                                                                     |
| `storagePersisted`                                                                                            | **latest `clientUpdatedAt` wins** — a per-device capability flag, not something to union                                                                                                                                                             |
| `dailyCompletion`                                                                                             | **latest by date, then latest `clientUpdatedAt`** — date-scoped per its own doc comment in `schema.ts`; a stale date from either side means "not completed today," so compare dates first, only fall back to recency on a genuine same-day tie       |
| `rushStats`                                                                                                   | **non-null wins over null; else max/latest per sub-field** per the original plan's rule (bests = max, `lastRunAt` = max, counts = max — stated trade-off: counts are monotonic per device but not summable across devices without double-count risk) |
| `bestRunStreak`                                                                                               | **max**                                                                                                                                                                                                                                              |
| `bossStats`                                                                                                   | same shape as `rushStats` — non-null wins over null, else max/latest per sub-field                                                                                                                                                                   |
| `missionProgress`                                                                                             | **latest `clientUpdatedAt` wins**, non-null wins over null — an in-progress run is inherently single-device state; merging two in-progress runs field-by-field would produce a run that never happened on either device                              |
| `missionStats`                                                                                                | same shape as `rushStats`/`bossStats` — max/latest per sub-field                                                                                                                                                                                     |
| `preferences`                                                                                                 | **latest `clientUpdatedAt` wins** — true settings, last-write-wins per the original plan                                                                                                                                                             |
| `anonId`                                                                                                      | **keep local** — never overwritten by a pulled value (matches T7's server-side "first-write-wins, written once" rule; the client-side mirror of that same invariant)                                                                                 |
| `challengerName`                                                                                              | **latest `clientUpdatedAt` wins**                                                                                                                                                                                                                    |
| `firstRunCompleted`                                                                                           | **OR** (true wins) — once true on any device, it should read true everywhere; there's no legitimate path back to false                                                                                                                               |
| `attempts` (sibling of `UserProfile` in `ExportedData`, not a profile field, but part of the same merge pass) | **union by `id`**, then re-sort by `createdAt`; never drop — the union feeds the `rating`/`ratedAttemptCount`/`streak` recompute above, so this merge has to run before those, not after or in parallel                                              |
| unknown fields (older client, newer payload)                                                                  | **preserved verbatim, round-tripped** — F9; the exhaustive `Record` type only covers fields this client's `schema.ts` knows about _today_; anything else on the wire passes through untouched                                                        |

**Schema-skew policy** (unchanged from the original plan, restated because it's the part most likely to get "simplified" under deadline pressure):

| Pulled blob vs. local `CURRENT_SCHEMA_VERSION` | Action                                                                                                                                                                                                    |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `blob < CURRENT`                               | Run `src/storage/migrations.ts`'s existing `runMigrations()`/`MIGRATIONS` chain over the blob first — the same path an old IndexedDB read or an old import file already takes. No sync-specific migrator. |
| `blob === CURRENT`                             | Merge normally, per the table above.                                                                                                                                                                      |
| `blob > CURRENT`                               | **Read-only sync**: do not merge, do not push. Surface a "reload to update" notice (T8's job to wire; T6 just needs to return a distinguishable result for this branch).                                  |

**Property tests** (`fast-check`, already a devDependency): `merge(a, a) = a`; `merge(a, b) = merge(b, a)` for the union/max fields; merge never decreases an attempts count or a rating-affecting best; **new — I11's idempotence**: `merge(merge(a, b), b) = merge(a, b)`. Fixtures: one fresh-install profile, one long-lived profile, one post-mission profile — **these need to be real exports, not hand-typed objects** (get them from Thomas's own dev profile via the existing `exportData()` / Settings export button, or a throwaway test account played through a mission — either way, before this task starts, not improvised mid-task).

**DoD:** exhaustive `Record<keyof UserProfile, MergeRule>` compiles; all three skew branches tested in both directions; unknown-key round-trip test; I11 idempotence test; zero imports from `src/storage`'s IndexedDB layer (`db.ts`), `fetch`, or `@clerk/react` in `merge.ts` — it takes two `ExportedData`-shaped objects in, returns one out, nothing else.

## T7 — sync endpoints, server half (1 session)

**Files:** `workers/src/routes/profile.ts` (or inline in `index.ts` if the file stays small — match the existing convention, `report.ts` is the closest precedent), `workers/test/profile.test.ts`. No migration needed — the `profiles` table and `profileStore.ts` already exist.

`PUT /api/profile` — body `{ schemaVersion, payload, clientUpdatedAt, baseRevision, anonId? }`. Optimistic concurrency: `UPDATE profiles SET revision = revision + 1, ... WHERE clerk_user_id = ? AND revision = ?` (conditional on `baseRevision`), check `meta.changes === 1` (F10 — a statement that matches zero rows is not success), else `409` with the current server state (fetched via `profileStore.get()`). First `PUT` for a user creates the row (`revision = 1`). Payload cap: **256 KB on the decompressed JSON**, checked before `profileStore.put()` does its internal compression (per the 2026-08-31 amendment — F11's cap and S1's compression are two different layers, don't conflate them). `anonId`: written only if the column is currently null — first non-null value wins, never overwritten after.

`GET /api/profile` — `profileStore.get(userId)`, `404` if no row yet, else `{ revision, schemaVersion, payload, updatedAt }`.

Row creation for `users`: already handled by T3's lazy-insert-on-first-authenticated-write — T7 doesn't duplicate that logic, just calls into the existing `clerkAuth()` middleware and trusts `c.get('userId')` the same way `account.ts`'s `DELETE` handler already does.

**DoD:** conflict path tested (two writers racing from the same `baseRevision` — exactly one 200, one 409 with current state); a real 257 KB payload test (rejected) and a 256 KB one (accepted); authz fixture reused from T3/`account.test.ts` (user A's token can't read or write user B's profile row — this is a `WHERE clerk_user_id = ?` away from being automatic, but write the test anyway, I5 doesn't accept "obviously correct"); `anonId` first-write-wins tested (second `PUT` with a different `anonId` doesn't change the stored one); `404` on `GET` for a user with no profile row yet.

## T8 — sync orchestration, client half (1–2 sessions — the one with a real open design question)

**Files:** `src/sync/engine.ts`, `src/sync/queue.ts`, tests.

**Open design question, decided here rather than discovered mid-task:** the plan's original T8 language ("flows through the normal storage listeners") assumes a pub/sub layer over `src/storage/` that **does not exist** — `saveProfile()` is called directly by feature code (`BossPage.tsx` and siblings) with no event emitted. Two real options:

1. **Wrap `saveProfile`**: add an optional module-level subscriber list inside `src/storage/profile.ts` itself (`onProfileSaved(cb)`), fired at the end of `saveProfile()`. Minimal surface change to a locked, well-tested file — but it does mean touching `profile.ts`, which the 5.1-era convention (see `exportImport.ts`'s own doc comment: "do not modify the storage functions... if you find yourself wanting to, that's a finding to report") treats as something to be careful about, not forbidden.
2. **Call sites opt in**: every profile-mutating boundary (attempt recorded, run ended, settings changed) also calls `syncEngine.notifyMutation()` explicitly, right next to its `saveProfile()` call. No change to `src/storage/`, but it's an easy-to-miss convention — a new boundary that calls `saveProfile()` and forgets the sync hook silently drifts out of sync (no test catches a _missing_ call the way it'd catch a wrong one).

**Recommendation: option 1**, with a single grep-able test (same shape as F13's SW-denylist assertion and T2a's `payload` guard) asserting every call site that saves a profile-mutating boundary also triggers a notify — cheapest to keep honest going forward, and it's one function's internals, not new IndexedDB surface. Settle this explicitly before writing `engine.ts`; it's the one place this task can quietly turn into a game of whack-a-mole across every feature page that calls `saveProfile`.

**Behavior:**

- **Pull on boot** when signed in — non-blocking (I2): app renders from IndexedDB immediately, the pull's merge result (if any) flows through the same save path as any other local mutation.
- **Push**, debounced ~5s, after the mutation-notify hook above fires.
- **Retry queue** (`src/sync/queue.ts`): exponential backoff, capped, persisted across reload (a small IndexedDB store or `localStorage` — pick one, record why; the queue itself is disposable UI-adjacent state, not app data, so `localStorage` is defensible per the same tier `useSignupPrompt.ts` already uses), drained on the `online` event.
- **Every failure path degrades to exactly-v3 behavior silently** (I2) — a failed push doesn't block play, doesn't surface an error to the user mid-puzzle, just requeues.
- **First-sign-in migration is the ordinary flow, not a special case**: local profile + server `404` → push as revision 1; server profile + fresh device → pull then merge (T6's laws handle an empty local profile merging cleanly — verify this is actually true for the _current_ `createDefaultProfile()` shape, not assumed from the original plan's confidence). Link `anonId` on that same first push.
- **Telemetry**: `sync_push`/`sync_pull`/`sync_conflict` counts only — no payload contents (I4).

**DoD:** airplane-mode pass (I2 — signed in, offline, behaves exactly like v3); reload-with-pending-queue test; **I12** (concurrency: pull and push in the same tick don't interleave against a partial state); two real devices against the dev env, recorded in this task's own amendment; a `sync_conflict` actually provoked and observed resolving (not just unit-tested); **anonymous → account migration replayed against a real pre-v5 export file** (get one before this task starts — see the fixtures note under T6); both schema-skew branches exercised end-to-end against the dev env. **See "Verification discipline" below — the two-real-devices pass is a real manual click-through, not a scenario with an equivalent passing test.**

## Coverage — Phase 5.2's build-plan DoD lines

| Build-plan DoD bullet                                                         | Task                         |
| ----------------------------------------------------------------------------- | ---------------------------- |
| Two-device test; offline-both, reconnect, provably lossless                   | T8                           |
| Anonymous → account migration keeps rating + history vs. a real pre-v5 export | T8                           |
| Airplane-mode pass identical to v3                                            | T8 (I2)                      |
| Schema-version skew defined and tested both directions                        | T6 (policy), T8 (end-to-end) |

## Footgun additions for this phase

| #   | Footgun                                                                                                                        | The mistake                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Prevented by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F24 | **Recomputing `rating`/`streak` from a partially-merged `attempts` array**                                                     | T6's field table has `attempts` uniting first, then `rating`/`ratedAttemptCount`/`streak` recomputing from the _result_. Doing them in field-declaration order instead of this dependency order produces a rating computed from only one device's history                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The merge function's own internal ordering is asserted by a test: recompute a rating from a two-device union and confirm it differs from (and is correct relative to) either single-device recompute                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F25 | **A missing `notifyMutation()` call at a new boundary**                                                                        | Every new feature page that calls `saveProfile()` directly is a place sync can silently stop happening for that boundary, with no failing test — precisely the risk the "option 1 vs. option 2" design question above exists to close off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Wrapping `saveProfile()` itself (option 1) removes the possibility structurally; if option 2 is chosen instead, a grep-based static test enumerating known `saveProfile()` call sites against known `notifyMutation()` call sites is not optional                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F26 | **Trusting `createDefaultProfile()`'s current shape to merge cleanly against real data without checking**                      | The original plan asserts "empty local merges cleanly by T6's laws" as a given. It's true only if every field's merge rule actually degrades correctly against `createDefaultProfile()`'s specific values (e.g. is `rating` in a fresh profile `0`, or some other sentinel that a max/recompute rule handles wrong?)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | T6's fixture set explicitly includes a fresh-install export (not a hand-typed stand-in for one), and the property tests run against it same as the other two fixtures                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F27 | **Testing the merge engine only against synthetic fixtures**                                                                   | Hand-typed test objects can't be wrong about production data's actual shape — they encode the author's assumptions about that shape, which is exactly the risk `anonId`/`challengerName`/nullable-field edge cases live in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | T6 and T8 both require _real_ exported fixtures (T6's DoD names it, T8's DoD requires a real pre-v5 export specifically) — get them before either task starts, not during                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F28 | **A retry queue that survives reload but not a schema bump**                                                                   | A push queued before a client update, replayed after, could carry a `schemaVersion` the _server_ has never seen from this path if the queue itself isn't schema-version-aware                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Queue entries store the `schemaVersion` they were built against; the drain path re-checks it against current `CURRENT_SCHEMA_VERSION` before replaying, not just before building                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F29 | **`codoro-dev`'s authenticated routes are unreachable by any real browser flow today** — **RESOLVED, see T7b amendment below** | Found doing T7's own "curl a real token against `codoro-dev`" DoD step: `clerkAuth()` requires the token's `azp` claim to match `APP_ORIGIN`, hardcoded to `"https://getcodoro.com"` in both wrangler.jsonc envs. A backend-minted real Clerk token (`sessions.createSession`/`getToken`, Clerk's own documented testing primitive) carries no `azp` at all and 401s correctly — that's `clerkAuth()` working as designed, not a bug. But no real browser session can satisfy the check either: nothing hosted at `https://getcodoro.com` talks to the _development_ Clerk instance (the real frontend there is wired to _production_ Clerk + the production Worker route; dev has no route of its own, per this same file's 2026-09-11 wrangler.jsonc amendment, and no separate dev frontend origin exists). A token with `azp=https://getcodoro.com` can only come from the production Clerk instance, which dev's `CLERK_JWT_KEY` (the _development_ instance's public key) can never verify. The two requirements are mutually exclusive as currently wired — this is a pre-existing gap in dev's env config, not something T7 introduced, and it silently blocks **T8's own DoD**, which names "two real devices against the dev env" as a required manual pass | **Fixed by T7b** (2026-09-13): `APP_ORIGIN` → `APP_ORIGINS`, a comma-separated allow-list; dev's list includes `http://localhost:5173`, and a Vite dev-server proxy (`vite.config.ts`) keeps the browser's real origin at `localhost:5173` while forwarding `/api/*` to the deployed dev Worker. A genuine browser sign-in against `codoro-dev` now works end-to-end — see the T7b amendment at the bottom of this document.                                                                                                                                                                                                                                                                                                                                                                |
| F30 | **The deployed `codoro-api-dev` Worker's `CLERK_JWT_KEY` secret didn't match the value in `workers/.dev.vars`**                | Found during T7b Piece 3, after fixing F29: a real, fresh, correctly-`azp`'d browser token still 401'd against the _deployed_ Worker, while the identical token verified fine (i) locally via `@clerk/backend`'s `verifyToken()` using `.dev.vars`' copy of `CLERK_JWT_KEY`, and (ii) against a local `wrangler dev --env dev` (which reads secrets from `.dev.vars`). Since the only variable between (ii) and the deployed Worker is which `CLERK_JWT_KEY` value each has, the deployed secret was stale/wrong — likely never updated after some earlier key rotation or a bad initial `wrangler secret put`. Piece 0's own signature check (T7b) only ever tested the _local_ `.dev.vars` copy against a backend-minted token; it never round-tripped through the actually-deployed secret, so it couldn't have caught this                                                                                                                                                                                                                                                                                                                                                                                                                                        | Re-uploaded the correct value from `.dev.vars` via `wrangler secret put CLERK_JWT_KEY --env dev`; confirmed fixed by the same real-token test immediately after (401 → 404 "no profile row yet", the correct next response). No code change — a deploy-environment data-hygiene gap, not a bug in `auth.ts`. Worth a standing note: **a secret value can't be diffed against `.dev.vars` from the outside** (`wrangler secret list` only proves a name exists, never a value) — the only way this surfaces is a real end-to-end request failing when everything else says it should succeed. If a future rotation of the dev Clerk instance's JWKS key happens, re-running Piece 3's exact real-token round-trip (not just Piece 0's local-only check) is what would catch a repeat of this |

## Verification discipline — a real click-through, not just green tests

**Why this is its own section, not just a DoD bullet:** 5.1's own history is the argument. T5 shipped with `pnpm validate` green and 2616 passing tests, its DoD boxes checked from local-bindings evidence — and then needed seven follow-up fixes (#127–#133) once Thomas actually clicked through sign-up on production, including one _critical_ crash on live `/settings` and a verification email that had never once actually sent. None of that was a testing failure in the narrow sense — the unit and integration tests that existed were accurate about what they covered. The gap was that nothing in the loop exercised the real, wired-together path (real Clerk instance, real browser, real network) before calling the task done. Automated coverage and "someone actually clicked through it" caught different bugs; 5.1 only had the first until after merge.

5.2 has more surface area for this exact failure mode than 5.1 did, not less: T8 is the first task where the client talks to the Worker on every normal play session, not just at auth boundaries, and merge bugs are the kind that hide behind passing unit tests built from the merge author's own assumptions (F27, above) right up until two real devices produce a real conflict.

**Concretely, add a manual click-through pass to each task's own DoD, distinct from and in addition to its automated tests:**

- **T6**: no click-through of its own (it's pure logic, no I/O) — but its fixtures must be real exports (already required by its DoD and F27), which is this task's version of the same discipline: real data, not the implementer's mental model of real data.
- **T7**: attempted 2026-09-12, immediately after merge — deployed `codoro-dev` was confirmed live (`/api/health` over real network), and a real Clerk-signed token was minted against the development instance via the Backend API (`sessions.createSession`/`getToken`, Clerk's own documented mechanism for exactly this kind of test) and sent as a real `Authorization: Bearer` header to the real deployed Worker. It 401'd — correctly, per **F29** below: the token has no `azp` claim, and `clerkAuth()`'s `authorizedParties` check requires one. Digging into _why_ surfaced F29 itself, a real pre-existing gap in how `codoro-dev` is wired, not a T7 bug: no real browser flow can currently produce a token that satisfies this check against dev at all. This still exercised the real network path, the real deployed code, and real Clerk signature verification (not a locally-forged test JWT) — it just couldn't reach a positive-path (200/201) result given F29. Not pursued further this session (reverse-engineering Clerk's Frontend API ticket-redemption flow to fabricate a matching `azp` was judged not worth the risk/cost of several more blind attempts) — logged as F29 instead of forced to a false green.
- **T8**: this is the one that matters most. Before signing off T8's DoD, do the two-real-devices pass _as an actual manual session_ — two browsers (or a phone + a laptop), signed into the same account, played offline-and-reconnected on purpose, watched for a real `sync_conflict` to resolve. **Reverted by T7b (2026-09-13):** F29 is resolved (see the footgun table and the T7b amendment below) — this now runs against `codoro-dev` itself, as originally intended, via `pnpm dev` + the Vite dev-server proxy (`vite.config.ts`) pointed at the deployed dev Worker (`DEV_API_TARGET`), not against `getcodoro.com`/production Clerk. The production-origin workaround this bullet briefly named was exactly that — a workaround for F29 — and should not outlive it. Screenshot or otherwise record what was seen, same convention as the amendments throughout this plan and its predecessor. If the LAN-IP origin path (`wrangler deploy --env dev --var APP_ORIGINS:"http://localhost:5173,http://<lan-ip>:5173"`) turns out not to work for a real second device, that is itself a finding for T8's own DoD, not something to route around.
- **Phase close-out**: before writing 5.2's closing amendment, do one more full pass of the actual user-facing flow this phase adds — sign in on a fresh device with existing play history, confirm it shows up, go offline, play, reconnect, confirm nothing was lost — the same shape of pass that would have caught #128–#131 in 5.1 before merge instead of after.

This isn't a call for more tests; T6/T7/T8's automated DoDs above are already thorough. It's a standing rule that a phase isn't done until someone has actually used the thing it built, once, for real — separate from and after the automated suite is green.

**Merge rule:** a PR merges when CI is green, a fresh-context review has run, and either its manual click-through has run or the change is unreachable by users and the PR body says so. Nothing reaches production without its own deploy task with its own DoD — a merge to `main` is not itself a production release, and a task's DoD isn't satisfied by "the PR merged."

## Suggested sequencing

T6 first, alone — it's pure logic, has zero dependency on the server or on 5.1's remaining loose ends, and every other task in this phase depends on its merge-rule table being settled. T7 next (small, mechanical given `profileStore.ts` already exists). T8 last, and budget it at the high end of 1–2 sessions — it's the task with a real open design question (the `saveProfile` hook point), the one requiring two real devices against the dev env, and the one requiring a real pre-v5 export fixture that doesn't exist yet. Piece 0 (closing out 5.1) can run in parallel with T6, or immediately before it — either way, before T8's DoD is signed off, since T8's own amendment is where "is 5.1 actually done" stops being an open question by simply not needing to be one anymore.

## T7b amendment (2026-09-13) — F29 resolved, `codoro-dev`'s authenticated path proven end-to-end

Ran before T8, per the T7b prompt. One session, Sonnet.

**Piece 0 verdict: F29 CONFIRMED, not mis-diagnosed** — but a second, independent bug (F30) was found downstream of it during Piece 3 and is recorded separately rather than folded into F29's text, since it has nothing to do with `azp`.

- Minted a real token against the development Clerk instance via the Backend API (`sessions.createSession`/`getToken`), exactly as T7 did. Decoded payload: `azp: undefined`, `iss: https://eager-cub-2880.clerk.accounts.dev`. Deleted the disposable test user afterward.
- `verifyToken(token, { jwtKey: <dev CLERK_JWT_KEY>, authorizedParties: [] })` **verified successfully** (`sub` matched) — signature and the dev JWKS key are good. The same token against `authorizedParties: ['https://getcodoro.com']` failed with exactly `Invalid JWT Authorized party claim (azp) undefined. Expected "https://getcodoro.com".` — F29's diagnosis confirmed precisely: `azp` is the sole blocker for this call.
- `wrangler secret list --env dev` on the deployed Worker: both `CLERK_JWT_KEY` and `CLERK_SECRET_KEY` present. `curl https://codoro-api-dev.codoroapp.workers.dev/api/health` → `{"ok":true,"version":"dev","clerkInstance":"development"}`.

**Piece 1 — `APP_ORIGIN` → `APP_ORIGINS`:** done as specified. `workers/src/env.d.ts`, `workers/src/auth.ts` (parses once per request, fails closed on an empty/whitespace-only list — never falls through to an unrestricted `authorizedParties: []`), `workers/wrangler.jsonc` (`production.vars.APP_ORIGINS = "https://getcodoro.com"`, unchanged in substance; `dev.vars.APP_ORIGINS = "http://localhost:5173"`, with the LAN-origin `--var` deploy-time convention documented in a comment, deliberately not committed). Five new tests in `workers/test/auth.test.ts` (second-entry-of-two match, no-`azp` regression, no-match-in-multi-entry-list rejection, empty/whitespace-list rejection) plus the pre-existing single-entry tests updated. New static guard `workers/test/static/appOriginsGuard.test.ts` asserts production's `APP_ORIGINS` parses to exactly `["https://getcodoro.com"]`, using `ts.parseConfigFileTextToJson` (the already-installed `typescript` package's own tolerant JSONC parser — no new dependency, and safe against `wrangler.jsonc` values like `"https://getcodoro.com"` that a naive `//`-strip would corrupt). `workers`' `pnpm validate` (typecheck + lint + test) is green: 70 tests passing, 12 test files.

**Piece 2 — Vite dev proxy:** done as specified in `vite.config.ts` (`server.proxy['/api']`, `changeOrigin: true` with a comment on why it doesn't touch `azp`). `DEV_API_TARGET` documented in `.env.example`, no `VITE_` prefix. `pnpm build` re-run and confirmed unaffected (same output, `server.*` config only applies to `vite dev`); `src/app/routes.test.ts`'s `vite.config.ts` drift guard still passes.

**Vite/`.env` finding, not in the original task list:** `process.env.DEV_API_TARGET` inside `vite.config.ts` is **not** populated by placing `DEV_API_TARGET=` in the repo's `.env` file — Vite (8.1.4) does not inject non-`VITE_`-prefixed `.env` values into the config file's own `process.env` (only `import.meta.env` in client code gets `.env` values, and only `VITE_`-prefixed ones). It must be a real shell/OS environment variable for the config to see it (`DEV_API_TARGET=... pnpm dev`, or `export`ed beforehand). `.env.example`'s comment doesn't promise otherwise, but it's worth stating plainly here since the natural reading of "documented in `.env.example`" is "works if placed in `.env`," and for this var it doesn't.

**Piece 3 — manual click-through, real numbers:**

1. `VITE_CLERK_PUBLISHABLE_KEY` for the click-through was derived from the dev instance's known frontend API host (`eager-cub-2880.clerk.accounts.dev`, seen in Piece 0's token `iss`) per Clerk's documented `pk_` encoding (`pk_test_` + base64 of `"<frontend-api-host>$"`) — no dashboard access was needed or used. Set only in the local gitignored `.env` for the duration of this task and removed afterward.
2. `pnpm dev` (Vite proxy → `DEV_API_TARGET=https://codoro-api-dev.codoroapp.workers.dev`), signed in at `http://localhost:5173` through the real dev Clerk instance with a fresh disposable test account (`tshore2004+t7bpiece3@gmail.com`, verified via a real 6-digit email code, deleted afterward through the app's own "Delete account" flow — same account-hygiene discipline as Piece 0 and T7's own click-through).
3. Decoded `getToken()`'s output in-session: **`azp === "http://localhost:5173"`**, confirmed.
4. First attempt at `GET /api/profile` 401'd even with a correct `azp` — this is **F30** (see the footgun table): the _deployed_ Worker's `CLERK_JWT_KEY` secret didn't match `.dev.vars`. Root-caused by comparing the same real token against (a) local `verifyToken()` with `.dev.vars`' key — passed, (b) a local `wrangler dev --env dev` instance — passed (`404`, correct), (c) the deployed Worker — failed (`401`), isolating the deployed secret as the only differing variable. Fixed with `wrangler secret put CLERK_JWT_KEY --env dev` (value from `.dev.vars`).
5. Re-ran the sequence against the deployed dev Worker through the real proxy path, freshly authenticated each call via `Clerk.session.getToken()`:
   - `GET /api/profile` → **404** `{"error":"Not found"}`
   - `PUT /api/profile` → **201** `{"ok":true,"revision":1}`
   - `GET /api/profile` → **200** `{"revision":1,"schemaVersion":13,"payload":{"hello":"t7b-piece3-verify","ts":1789311744},"updatedAt":1789311760360}`
   - Confirmed via `/api/health` throughout: `clerkInstance: "development"`, i.e. this was the deployed dev Worker, not a local one.
   - Screenshot of the signed-in Settings state taken and saved alongside this session's records.
6. Also redeployed `codoro-api-dev` once mid-task (`pnpm run deploy:dev`) so the deployed Worker actually ran Piece 1's new `APP_ORIGINS`-parsing code and value — the first attempt (before this redeploy) 401'd for the old reason (stale `APP_ORIGIN` var), which is expected and not a new finding.

**DoD:**

- [x] Piece 0's finding written down — **F29 confirmed**.
- [x] `pnpm validate` (workers) green — 70 tests, 12 files. Root `pnpm validate` (the combined `typecheck && lint && test && validate:content && build && workers validate` script) OOM'd on this machine's default Node heap partway through `eslint .` — re-ran each step individually with `NODE_OPTIONS=--max-old-space-size=8192`: typecheck clean, lint clean except two pre-existing parse errors on stray `.claude/worktrees/og-card-challenge/.wrangler/tmp/*` build artifacts (unrelated to this branch, not touched by it) and one pre-existing `react-refresh` warning in `AuthProvider.tsx`, `validate:content` clean, `pnpm build` clean (Piece 2's byte-identical-build check). `pnpm test` (root) had 2 failures in `PuzzleCardShell.test.tsx`, a file untouched by this branch (confirmed via `git status`/`git diff`) — re-ran that file alone and it passed 34/34, so this was resource-contention flakiness under the full-suite run, not a real regression.
- [x] The five `authorizedParties` tests, including the no-`azp` regression test.
- [x] The static guard on production's `APP_ORIGINS`.
- [x] A real browser 201 from `PUT /api/profile` against the deployed `codoro-api-dev`, recorded above (verbatim status codes and bodies).
- [x] Plan amendment: F29 marked resolved; the Verification discipline T8 bullet reverted to run against `codoro-dev`.
- [x] Two-device note: **only the localhost path was exercised in this task's original session.** The LAN-origin path was attempted in the closeout session below — see "Closeout addendum," which supersedes this line.

**Also fixed, not in the original task scope:** F30 (deployed `CLERK_JWT_KEY` secret was stale) — see the footgun table above. This was blocking in a way F29's fix alone would not have surfaced until T8 tried the same real-browser path and hit the same wall from one layer deeper.

## T7b closeout addendum (2026-09-13) — F30 detector, PR landed, LAN check, pre-v5 fixture question

Four items, run in order after the T7b amendment above. Not T8 — explicitly held back.

### 1. F30 detector

`/api/health` now reports `clerkJwtKeyFingerprint` (first 8 hex of SHA-256 of `CLERK_JWT_KEY`'s PEM, alongside the existing `clerkInstance`) — a public key, so this discloses nothing. Two new tests in `health.test.ts`: one recomputes the fingerprint independently from whatever `CLERK_JWT_KEY` the test environment actually has (not hardcoded — CI has none, per F5/F7), the other asserts the fingerprint actually changes when the key does. `workers` `pnpm validate`: 72/72. Sanity-checked against the real deployed dev Worker post-redeploy: `clerkJwtKeyFingerprint: "3b0879cc"`, matching an independent local hash of `.dev.vars`' `CLERK_JWT_KEY` — the detector works, and confirms no new drift since the F30 fix.

### 2. Landed

Everything above plus the F30 detector is committed (`1172c4a`) and pushed to `docs/t7-dev-auth-gap-finding`; PR **[#141](https://github.com/codoro-app/codoro/pull/141)** updated with title/body covering F29's confirmation, the `APP_ORIGINS` change + static guard, F30 + its detector, and the honest caveats. Not merged — that's Thomas's call.

### 3. LAN-origin check — real result, not fully resolved

Redeployed `codoro-api-dev` with `APP_ORIGINS: "http://localhost:5173,http://192.168.1.71:5173"` (this machine's real LAN IP). Ran `vite --host` (bound to `0.0.0.0`, reachable at `http://192.168.1.71:5173`). No literal phone was used — Claude has no physical device — but per the task's own framing ("the phone is realism, not coverage"), the actual technical question (does Clerk's dev instance accept a plain-`http`, non-localhost origin at all) was tested directly: a browser was pointed at `http://192.168.1.71:5173/settings` instead of `localhost:5173`.

**What worked:** the page loads normally at the LAN origin, and Clerk's own sign-in/sign-up UI mounts and accepts input with no origin-level rejection, no CORS error, no client-side "invalid origin" message. This is a real, positive signal — Clerk does not appear to categorically refuse this origin shape.

**What didn't get resolved:** account creation on this origin triggered a Cloudflare Turnstile ("Verify you are human") challenge that did not appear during the equivalent flow on `localhost:5173` in Piece 3. Whether this is genuinely origin-triggered risk scoring or incidental (a second automated sign-up from the same IP in one session, a fresh/lower-trust browser profile, etc.) **could not be determined this session** — completing a CAPTCHA is outside what Claude will do (policy: never solve/bypass bot-detection), so the sign-up could not be carried through to a token, and no `PUT /api/profile` call was made from this origin. This is being reported plainly rather than forced, per the task's own instruction.

**Also confirmed, as expected and already flagged by the task doc:** `window.isSecureContext === false` and `'serviceWorker' in navigator === false` on the LAN http origin — no service worker will register there. Not a new finding, just confirmed directly.

**State left for a human follow-up pass:** `codoro-api-dev` is deployed right now with both origins in its allow-list; `vite --host` was running at `http://192.168.1.71:5173` for the duration of this check (stopped after, to not leave a dev server listening unattended — restart with `DEV_API_TARGET=https://codoro-api-dev.codoroapp.workers.dev npx vite --host`, `VITE_CLERK_PUBLISHABLE_KEY` set to the dev `pk_test_…` key). Windows Firewall is enabled on all profiles on this machine — an actual phone on the LAN may additionally need an inbound allow rule for `node.exe`/port 5173 that this session did not create (modifying firewall/security settings is outside what Claude will do unilaterally). A real phone, held by a person who can tap through a CAPTCHA, is the only way to find out whether that Turnstile challenge is a real per-origin blocker or incidental — **this is now explicitly T8's problem to resolve, not assumed away.**

### 4. Pre-v5 fixture question — settled, no fixture needed (for now)

Checked before building anything, per the task's own instruction:

- `CURRENT_SCHEMA_VERSION` in `src/storage/schema.ts` was **already 13** at `9e2d09a` (`feat(workers): v5 Phase 5.0 T1`, 2026-09-10) — the very first commit of Phase 5.0, i.e. the start of v5. It is still 13 at `HEAD`. **Zero schema-version bumps have happened anywhere in v5's history so far** (T1 through T7b).
- `src/storage/exportImport.ts` (the wire-format module `ExportedData` lives in) has **zero commits and zero diff** between `9e2d09a` and `HEAD` — the export/import shape itself hasn't been touched during v5 either.
- All three of T6's existing fixtures (`fresh-install.json`, `long-lived.json`, `post-mission.json`, all generated 2026-09-12) carry `"schema_version": 13` — identical to what a genuine pre-v5 export would carry, because the number hasn't moved.

**Conclusion: the requirement collapses.** A "pre-v5 export" and a "current export" are structurally identical right now — there is no schema skew to exercise, and building or fetching a separate `pre-v5.json` fixture today would test nothing that the three existing fixtures don't already cover. F27's actual intent (real data, not synthetic) is already satisfied by those three.

**What would make this matter again:** the first schema-version bump landed during v5 (i.e., `CURRENT_SCHEMA_VERSION` going to 14). At that point, a fixture captured _before_ that specific bump — not necessarily pre-v5 in calendar terms, just pre-that-bump — is what T8's "both schema-skew branches exercised end-to-end" DoD line actually needs. No such fixture exists yet because no such bump has happened yet. Re-check this exact question (git log on `CURRENT_SCHEMA_VERSION` since `9e2d09a`) before T8 signs off that DoD line, not before T8 starts.

## T8a amendment (2026-09-13) — client engine/queue, the four decisions written down

One session, Sonnet. Scope: everything in T8's DoD provable by automated tests
only. The two-real-devices pass, the provoked `sync_conflict` observed
resolving, and the airplane-mode click-through are **T8b** (a separate
session, a human at the keyboard) — not attempted here, per this task's own
instruction. Piece 0's two carry-overs from #141 landed first: `auth.ts`'s
`APP_ORIGINS` read is now type-guarded (`typeof rawAppOrigins === 'string'`)
so an absent/non-string value 401s instead of 500ing, with a regression test
(`auth.test.ts`, "entirely absent from env"); grepped the repo for a
remaining singular `APP_ORIGIN` reference — none, only doc-comment mentions
of the old name.

**Files built:** `src/sync/engine.ts`, `src/sync/queue.ts`, both fully
tested; `src/storage/profile.ts` gained `onProfileSaved()` (option 1, as
this plan already recommended); `src/storage/profileMutationGuard.test.ts`
is F25's grep guard; `src/telemetry/events.ts` gained
`trackSyncPush`/`trackSyncPull`/`trackSyncConflict`.

### Decision 1 — where `baseRevision` lives across reloads

`localStorage`, one JSON blob per device under `codoro:sync-meta`:
`{ userId, baseRevision }`. Sync metadata, not app data — same tier
`useSignupPrompt.ts` already uses for its own disposable, non-schema-versioned
local state. Read/written only by `engine.ts`; never exported/imported
alongside `exportData()`/`importData()` (it isn't part of `ExportedData` and
never should be — a revision number is meaningless on a different device).

### Decision 2 — the retry queue's persisted shape

`localStorage` under `codoro:sync-queue`, **one entry, not an array** —
`{ userId, schemaVersion, queuedAt, attempts }` (T8b removed the
originally-planned `nextRetryAt` field — see that task's own amendment
below, "queue backoff decision"). Deliberately
carries **no profile data at all**, only a marker that a push is owed: a
push, when it actually runs (whether from the 5s debounce, `flush()`, or a
drained retry), always calls `exportData()` fresh against whatever is
currently in IndexedDB — never a stale snapshot captured at the moment of
the original failure. This is why F28's schema-version check on the queue
entry is meaningful rather than vacuous: it's not "does this stale payload's
version match," it's "is the _intent_ to push still valid," because a client
update between the failed push and the drain means the ordinary
pull-then-merge cycle should re-establish sync at the new version instead of
blindly replaying old intent. `localStorage` over IndexedDB for the same
reason `useSignupPrompt.ts` chose it: this is disposable, UI-adjacent
scheduling state, not data whose loss the user would ever notice — losing a
queued retry just means the next mutation's debounce (or the next `online`
event, or the next boot's pull) re-establishes sync.

### Decision 3 — 409 handling, bounded

No change to `src/auth/api.ts`: `apiFetch`'s error path only preserves the
`{ error }` string (`errorMessageFrom`), not a conflict response's `current`
body, and extending `ApiError` to carry the full parsed body was considered
and rejected here — it would touch a locked, already-relied-upon module for
a need `engine.ts` can satisfy without it. Instead, a conflict triggers a
fresh, ordinary `GET /api/profile` (the same code path `pull()` already
runs) to fetch current server state, merge, and retry the `PUT` with the
new `baseRevision` — functionally identical to reading the 409 body, one
extra round trip, zero changes to a file outside this task's scope. Capped
at **3 total push attempts per `push()` call** (i.e. at most 2
conflict-triggered merge-and-retry cycles) — within the plan's "2–3" range,
tested exactly (`engine.test.ts`'s conflict-cap test asserts the 4th attempt
never happens and the call requeues instead of looping).

### Decision 4 — 413 is terminal, not retryable

`push()`'s per-attempt result has a distinct `too-large` kind; on it, `push()`
returns immediately without calling `enqueueRetry`/`recordQueueFailure` —
the entry never enters the queue at all. Telemetry records it as its own
`sync_push` outcome (`'too-large'`), distinguishable from a transient
network/5xx failure. Local state is untouched by construction: `push()` only
_reads_ local storage to build the outbound payload, so "leave local state
untouched" holds for every failure path, not just this one.

### F31 — account switching on one device (new footgun, promised above)

| #   | Footgun                                                                            | The mistake                                                                                                                                                                                                                                                                                                                                            | Prevented by                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F31 | **Account switching on one device carries the prior account's sync state forward** | Sync metadata (`baseRevision`) isn't keyed by user id, so signing into account B on a device that previously synced account A pushes A's stored `baseRevision` — and, if push ran before any pull, A's local profile — into B's row. The local-first IndexedDB profile does not clear on sign-out today (a separate, pre-existing, not-fixed-here gap) | `codoro:sync-meta` and `codoro:sync-queue` are both keyed by `userId`. `handleSignedIn(userId)` compares the stored `userId` to the incoming one: a mismatch discards both stores and always pulls before any push is allowed to run for the new identity (`engine.test.ts` tests both directions: same user returning keeps its meta/queue; a different user arriving discards them and pulls first) |

**Named limit at T8a time, not silently papered over:** this fixed
_stale-revision_ and _push-before-pull_ corruption. It did **not** clear
the local IndexedDB profile itself on an account switch — a device that
had account A's full play history locally, then signed into account B,
would still _merge_ A's local attempts into B's server state on the first
pull-then-push cycle (`merge()` has no concept of "this attempt belongs to
a different person" — it isn't given one). That was the pre-existing gap
this amendment's own text named, not a new one T8a introduced; flagged for
whoever picked it up next, rather than assumed solved.

**F31's second half — closed by T8b (2026-09-13), see that task's own
amendment below.** `handleSignedIn()` now resets local IndexedDB to a
blank profile (preserving this device's own `anonId`) the moment an
account switch is detected, and adopts a 200 response _wholesale_ rather
than merging it — never letting the prior account's local data reach the
merge path at all. A genuinely fresh account B (404) pushes this
now-blank state, not A's. An independent review found this fix was itself
incomplete on the first pass (stale in-memory React session state could
still resurrect A's data after a successful reset) — see the T8b
amendment's "review findings addressed" section for the fix (a forced
reload on a detected switch) and everything else the review caught.

### Also decided, smaller

- **`onProfileSaved` (option 1, as recommended above):** a module-level
  listener array inside `profile.ts`, fired at the end of `saveProfile()`
  only — not from `loadProfile()`'s internal migration/corrupt-recovery
  writes, which go through a private `putProfile()` helper, never through
  `saveProfile()` itself, and are self-healing local-storage concerns, not
  mutation boundaries a remote device needs to hear about.
- **F25's grep guard, kept per the recommendation even under option 1:**
  `src/storage/profileMutationGuard.test.ts` asserts, by reading the real
  source of every file that touches `PROFILE_STORE` directly, that only
  `profile.ts` and `exportImport.ts` (import/export's own, deliberately
  separate, wholesale-replace path — see that file's own doc comment) ever
  write to it — a regression guard against a _new_ direct-write bypass
  appearing that would silently skip the notify hook, structurally, not just
  "no call site currently misses it."
- **Merged attempts land locally via `appendAttempt()`, not a new storage
  primitive.** `merge()`'s `attempts` result is already the full union
  (never drops a row); looping it through the existing, already-public
  `appendAttempt()` upserts each by id (a no-op for rows already present) —
  no new file in `src/storage/` needed. `saveProfile(mergedProfile)` is
  called last, after every attempt upsert, matching I12's literal DoD
  language ("`saveProfile` is called with a fully-merged, never a partial,
  state").
- **I11 (idempotence) is enforced structurally, not just tested indirectly**:
  before merging, `doPull` compares the pulled `revision` to the stored
  `baseRevision` for the current user; an exact match short-circuits to a
  `noop` result with no merge, no `appendAttempt`/`saveProfile` calls at all,
  rather than relying solely on `merge()`'s own mathematical idempotence to
  make a redundant re-application harmless.
- **Pull/push mutual exclusion (I12)** is one `async` mutex
  (`withLock`) inside `engine.ts`'s closure — every public entry point
  (`pull`, `push`, and therefore `handleSignedIn`, `notifyMutation`'s
  debounced call, `flush`, `handleOnline`) chains onto the same promise, so a
  pull and a push requested in the same tick serialize rather than
  interleave. The conflict-retry's internal re-pull calls an _unlocked_
  internal `doPull`, not the public `pull()`, to avoid a non-reentrant
  deadlock while already holding the lock from `push()`.
- **Not built this session:** any React-level wiring (a `useSyncEngine` hook,
  `main.tsx` instantiating one engine, subscribing it to `onProfileSaved`/
  `window.addEventListener('online', …)`/`visibilitychange`). The brief's
  file list is `engine.ts` + `queue.ts` + tests; the app does not yet call
  any of this in production. **T8b needs this wired before its manual pass
  can run at all** — flagged here explicitly, not discovered mid-T8b.

### Independent review (fresh context, per this task's own instruction)

A subagent with no prior context reviewed the diff cold against I1–I12,
F24–F31, and the seven required questions. It found two real bugs (both
fixed, both regression-tested) and several smaller issues. Not
self-reviewed — a controller with the intent already in its head reads code
differently than one that has to derive it, which is the whole reason this
step exists.

**B1 — `merge()`'s own documented throw path wasn't caught.** `merge.ts`'s
doc comment says a non-migratable behind-schema blob throws and "T8 catches
it like any other pull failure per I2" — `doPull` didn't. Reached via
`push()`'s conflict-retry (which calls `doPull` while already inside the
lock), this was a genuine unhandled promise rejection on `void push()`, not
just a rejected `pull()`. **Fixed**: `merge(local, remote)` wrapped in its
own try/catch, degrading to `{ kind: 'error' }` like every other pull
failure. Regression test: a schema-behind, deliberately-invalid remote
blob (mirroring the reviewer's own repro) now resolves `pull()` to
`{ kind: 'error' }` instead of rejecting.

**B2 — the local/remote timestamp comparison was structurally broken.**
`merge()`'s "latest wins" rules compare `local.exportedAt` vs
`remote.exportedAt`, but the engine built `local` fresh via `exportData()`
on every pull/push (required by Decision 2 — the queue never carries a
stale snapshot), and `exportData()` stamps `exportedAt` with "now," not
"last real mutation." Since a remote's `exportedAt` is necessarily earlier
(it was already pushed once), `local.exportedAt` was _always_ `>=` it —
every latest-wins field (`preferences`, `challengerName`, `requeueState`,
`storagePersisted`, `missionProgress`, `dailyCompletion`'s tiebreak) could
never let remote win, in either direction, ever. Verified against a real
repro before fixing, not just argued from reading the code.

**Fixed** with a persisted local-mutation clock
(`LAST_MUTATED_AT_STORAGE_KEY`, `readLastMutatedAt`/`writeLastMutatedAt` in
`engine.ts`): `notifyMutation()` stamps it (unconditionally, even
signed-out — a guest's local changes must compare correctly once they
eventually sign in); a successful merge re-stamps it to whichever side's
`exportedAt` actually won, so the _next_ comparison anchors to a real event
instead of drifting back to "whenever sync last ran." `buildLocalExportedData()`
substitutes this recorded time for `exportData()`'s own "now" stamp when
present. Two regression tests reproduce the reviewer's exact scenario in
both directions (a genuinely older recorded local clock lets remote win; a
genuinely newer one keeps local).

**Named limit on the fix, not silently claimed complete**: this only fully
closes the bug once something calls `notifyMutation()` at the real mutation
boundary — which is exactly the `onProfileSaved` → engine wiring this
session does not build (see "Not built this session," above). Until T8b
(or whoever wires it) does that, a device's very first pull/push before any
merge has ever run still falls back to "now." The regression tests call
`notifyMutation()`/seed the storage key directly, the same way the eventual
wiring will, to prove the mechanism — not to claim the gap is closed today.

**S1 — the original I12 test was vacuous.** It asserted on a 3-microtask-tick
window; `doPushOnce`'s own real IndexedDB work (`exportData()`) can't
resolve inside three microtasks regardless of whether a mutex exists, so
the test passed even with `withLock`'s body replaced by `return fn()`
(mutex fully removed) — verified directly, then reverted. **Fixed**: the
assertion window is now a genuine 200ms real delay, long enough that an
unlocked implementation's PUT would have fired on its own if nothing were
serializing it. Re-verified: disabling the mutex again now fails this
specific test (`expected … to be called 1 times, but got 2 times`),
confirmed, then the mutex was restored and diffed byte-identical to before
the experiment.

**S2 — F31's "different user arriving" test proved a weaker claim than it
looked like.** It only asserted metadata/queue were cleared; because its own
pull failed (network error), it never reached the one path where the named
limitation actually bites — a genuinely fresh account B (server 404), where
`handleSignedIn`'s ordinary "not-found → push" rule still fires and pushes
whatever local profile this device is holding (account A's, if never
cleared on sign-out) as account B's revision 1. **Added**: a test that
reaches exactly that path and asserts the pushed payload actually is
account A's data — demonstrating the named, accepted limitation directly
rather than only asserting it in prose.

**S4 — a 413 didn't clear a queue entry that already existed from an earlier,
unrelated failure.** Decision 4's "never queued" held for _creating_ an
entry but not for a pre-existing one; left in place, a device reconnecting
(`handleOnline` drains unconditionally) would repeat the same guaranteed-413
round trip forever. **Fixed**: the `too-large` branch now also calls
`clearQueue()`. Regression test added.

**N4 — `schemaSkew` wasn't cleared on sign-out**, so `getSchemaSkew()` would
keep reporting a previous account's "reload to update" state after
`handleSignedOut()`. **Fixed** (one line).

**N1 — a misbehaving `onProfileSaved` listener could turn a successful
`saveProfile()` into a rejected one** for all 20+ existing call sites.
**Fixed**: `notifyProfileSaved` now isolates each listener in its own
try/catch. Test added (TDD: written first, watched fail, then fixed).

**S3, N2, N3, N5, N6 — documented, not changed this session** (parked, not
silently dropped):

- S3 — **resolved by T8b**, not merely parked: the retry queue's
  exponential backoff (`nextRetryAt`) had no consumer (`handleOnline` drains
  unconditionally, by design) and was never going to get one this phase, so
  T8b deleted the field outright rather than leaving it built-ahead-of-its-
  consumer. See that task's own amendment below, "queue backoff decision,"
  for the full reasoning.
- N2: the F25 grep guard (`profileMutationGuard.test.ts`) catches the two
  syntactic bypass shapes present in the tree today, not every conceivable
  one (a non-recursive scan, a regex that wouldn't catch a `const store =
tx.objectStore(...)` indirection). A regression guard against _known_
  bypass shapes, not a proof no bypass can ever exist.
- N3: `SyncPushOutcome`'s `'no-token'` member is currently unreachable —
  `doPushOnce` collapses a missing token into the same generic `'error'`
  path as a network failure, which is harmless (both silently retry) but
  imprecise telemetry.
- N5: the merged-attempts loop calls `appendAttempt` (its own IndexedDB
  open/write/close) once per attempt in the union, not just new ones — fine
  at this phase's fixture sizes, a real cost on a long-lived account's
  boot-time merge. Fixing it would mean a new bulk-upsert storage primitive,
  which this session's own scope note ("no new file in `src/storage/`
  needed") deliberately avoided adding.
- N6: `handleSignedIn` itself isn't wrapped in a single lock acquisition —
  it calls the locked `pull()` then the locked `push()` sequentially, so
  another operation could in principle interleave between them. Confirmed
  benign (a stray push just runs first and the following one re-bases
  correctly) but a real, named gap between the module doc's "every public
  entry point runs inside one mutex" and what `handleSignedIn` guarantees
  as an atomic unit.

## T8 handoff

**Proven, real evidence, ready to build on:**

- F29 is resolved. A real browser at `localhost:5173`, signed into the real dev Clerk instance, authenticates against the deployed `codoro-api-dev` end-to-end (`GET` 404 → `PUT` 201 → `GET` 200, real bodies recorded above).
- F30 is fixed and now has a standing detector (`/api/health.clerkJwtKeyFingerprint`) — a one-curl check against the same fingerprint computed locally from `.dev.vars` is the way to confirm dev's deployed secret hasn't drifted again, before or during T8.
- The pre-v5 fixture question is closed for now: no fixture work is needed unless/until the first schema-version bump lands. T6's three existing real fixtures are sufficient.
- `codoro-api-dev`'s `APP_ORIGINS` already includes both `http://localhost:5173` and this machine's LAN IP (`http://192.168.1.71:5173`) as of this closeout — T8 doesn't need to redo that deploy step, just confirm it's still current when T8 actually runs (deploys can drift; `wrangler secret list --env dev` + `/api/health` are the one-curl checks).

**Still unverified — T8 inherits these, not assumed-solved:**

- **The two-real-devices pass itself.** The localhost path works. The LAN-IP path loads and Clerk mounts, but a Cloudflare Turnstile challenge blocked completing sign-up from that origin this session, and it's not known whether that's origin-specific or incidental. T8's actual two-device DoD line needs a human with a real second device (or at minimum, a human willing to tap through a CAPTCHA) to find out — this session could not resolve it and did not force a green.
- **Windows Firewall** is enabled on this dev machine; whether a real phone can even reach `http://<lan-ip>:5173` over the network (separate from the Clerk-origin question) has not been confirmed. If T8's two-device pass fails to even load the page on a phone, check this before assuming it's another Clerk/origin issue.
- **What "changed" in T8's DoD, concretely:** the "two real devices against the dev env" line no longer needs the `getcodoro.com`/production-Clerk workaround (F29 is fixed) — but it also isn't a rubber stamp. Budget real time for it; the LAN-origin leg has an open question (Turnstile) that a plain re-read of this plan would have missed.

## T8b — sync wiring + review closeout (2026-09-13)

Closes the two review findings T8a's own amendment flagged (F31's second
half, schemaSkew not gating push), wires the engine into the app (T8a
explicitly left this undone — see "Not built this session," above), and
runs a real manual pass against `codoro-api-dev` and the real dev Clerk
instance. Reviewed cold three times, not self-reviewed, per this task's own
instruction.

### Part 1 — the two given findings, fixed

**F31's second half.** `handleSignedIn(userId)` now resets local IndexedDB
to blank (preserving this device's own `anonId` — the rule established at
T8a time: `merge()` has no concept of "this attempt belongs to a different
person," so reset-then-adopt, never reset-then-merge) the moment an account
switch is detected, and adopts a 200 response wholesale
(`adoptRemoteWholesale`) rather than merging it — the prior account's local
data never reaches the merge path at all. A genuinely fresh account (404)
pushes this now-blank state, not the prior account's. Regression tests
reach both the 200 and 404 paths directly and assert on the actual local
state left behind, not just on "meta cleared" (S2 above named exactly this
gap in T8a's own test and it doesn't recur here).

**Finding 2 — `schemaSkew` recorded but never gated anything.**
`getSchemaSkew()` existed since T8a purely as a read-only signal for future
UI; nothing stopped a push or a debounce-armed mutation once skew was
known, so a client running old code could keep hammering a schema-mismatched
server indefinitely. **Fixed**: `push()` early-returns before attempting
network I/O when `schemaSkew !== null`; the conflict-retry loop stops
(without requeuing) the moment a retry-pull itself reports skew;
`notifyMutation()` still stamps the mutation clock unconditionally (I11/I12
correctness doesn't care about skew) but returns before arming the debounce
timer when skew is known. Regression tests cover all three gates.

### Queue backoff decision (referenced from Decision 2, above)

T8a shipped `nextRetryAt`/`BASE_BACKOFF_MS`/`MAX_BACKOFF_MS` in
`queue.ts` with no consumer — `handleOnline()` has always drained
unconditionally, by design (Decision 2's own reasoning: a queued retry is
just "a push is owed," never a stale snapshot, so there's nothing an
exponential backoff would protect that immediate-drain-on-reconnect
doesn't already handle at this phase's scale). A periodic retry-drain timer
to actually consume `nextRetryAt` was considered and rejected: it would be
a second, independent trigger racing `handleOnline`/`visibilitychange`
through the same lock, for a scenario (server-side rate limiting a client
that's already online) this app doesn't have evidence of hitting. Built a
field ahead of a consumer that may never arrive is worse than not having it
— **the field was deleted outright** (`attempts` alone remains), rather than
left as unread, silently-rotting state. `queue.test.ts` updated to match;
no behavior change for any currently-reachable path.

### Part 2 — app wiring

`SyncEngineHost.tsx`, a render-nothing side-effect host: one `SyncEngine`
instance per mount (`useState`'s lazy initializer, not a ref — this repo's
`react-hooks/refs` lint rule forbids reading/writing a ref's `.current`
during render, which the classic lazy-ref-init idiom does). Five effects,
each independent so one listener's failure can't tangle another's:
`onProfileSaved` → `notifyMutation()`; Clerk loaded+signed-in →
`handleSignedIn(userId)` (covers both a real sign-in and "already signed in
at boot" — Clerk resolves `isSignedIn` the same way in both cases), reloads
the page if it reports `accountSwitchDetected` (closes the stale-in-memory-
session-state gap round 2's review caught, below); Clerk loaded+signed-out →
`handleSignedOut()`; `window` `online` → `handleOnline()`; `document`
`visibilitychange` hidden → `flush()`.

Mounted from three places, not one: `App.tsx`'s root host (gated on a new
`codoro:has-account` localStorage hint, `accountHint.ts` — I3/F8 discipline,
a device that's never had an account pays zero Clerk-boot cost, not even the
chunk fetch, since the hint gates the `lazy()` render itself, not just what
runs after it loads), plus `AccountSection.tsx`'s and
`SignupPromptSheet.tsx`'s own local hosts (gated on `!hasAccountHint` at
their own mount time, so a device's very first sign-in still gets live
wiring before the hint exists). Guest-first (I1/I2) verified, not assumed:
zero `/api/*` requests and zero console errors on a clean guest boot,
confirmed directly in a real browser against this round's code (see Part 3).

Bundle: `SyncEngineHost` (+ everything it statically imports — `engine.ts`,
`queue.ts`, `merge.ts`) builds as its own lazy chunk,
**11.12 kB / gzip 3.49 kB**, fetched only when actually rendered. Confirmed,
not assumed: grepped the built main entry chunk (`index-*.js`) for
`handleSignedIn`/`pending-adopt` — neither string appears; the only match
for `SyncEngineHost` in that chunk is the `import()` reference string
naming the lazy chunk's filename, not inlined engine code. The guest-path
critical bundle carries zero bytes of sync-engine logic.

### Independent review — three rounds, fresh context each time

**Round 1** found the two given findings (F31 second half, schemaSkew
gating) were fixable, but shallow as first implemented: the initial F31
fix reset local IndexedDB synchronously inside `handleSignedIn` without
first capturing this device's own `anonId` from the state being wiped,
risking a failure-mode where a crash mid-reset left neither the old nor a
correctly-anonId'd blank profile behind. Fixed by threading `deviceAnonId`
as an explicit parameter into `resetLocalToBlank`, read once before any
mutation starts.

**Round 2** found two blockers in the round-1 fix: (1) a stale-state
window — `handleSignedIn` resolving success meant local IndexedDB was
correct, but every already-mounted session hook (`usePracticeSession` and
its siblings) still held the _prior_ account's `UserProfile` in its own
React state, and the next ordinary `saveProfile()` call from any of them
would silently overwrite the just-adopted account with stale data. Fixed
by having `SyncEngineHost` force a full page reload on a detected switch —
the one guaranteed way to drop every such stale in-memory reference at
once — combined with a two-boot `localStorage` handshake
(`codoro:sync-pending-adopt`) so the reload happens _before_ any network
round-trip, closing the window where a stale save could land during the
switch itself, and the resumed boot knows what to finish adopting after
reload. (2) Wiring `SyncEngineHost` from three mount sites, each gated on
the same mutable `codoro:has-account` flag read at different times, opened
a real production-reachable sequence (sign out from Settings — the root
host's effect clears the hint but the root host itself never unmounts —
then a signup-prompt trigger fires elsewhere) where two instances mount at
once: two independent engines, two mutexes, racing pushes and potentially
both running the account-switch reset concurrently. Fixed with a
module-level singleton claim (`activeInstanceClaimed`), proven correct
(including StrictMode double-invoke) — only the instance holding the slot
ever wires up a listener or touches the network.

**Round 3** confirmed both round-2 blockers were genuinely closed, and
found three targeted marker-lifecycle edge cases in the two-boot handshake
itself — all fixed and covered by new regression tests, all in this
branch's final commit:

1. A pending-adopt marker for a _different_ user than the one signing in
   now is treated as a switch too, not silently ignored — closes a
   reachable "third account" sequence (a switch to B stalls offline before
   its adopt resolves; a third identity C signs in; without this, C's
   sign-in fell through to an ordinary merge against B's still-blank,
   mid-switch local state).
2. A schema-skewed resume no longer clears the pending-adopt marker —
   `adoptRemoteWholesale`'s skew branch returns before `importData()`/
   `writeSyncMeta()` ever run, so clearing the marker there was treating an
   unresolved switch as resolved.
3. The resume branch's `not-found` outcome now defensively re-resets local
   to blank (best-effort `anonId` read) before the fresh-account push,
   closing the narrow window between boot 1's original reset and the
   actual page unload where a stale `saveProfile()` from an
   already-mounted session hook could land.

No round-4 re-review was dispatched: round 3 found no blockers, only these
three should-fix items, all narrow and all covered by regression tests
exercising the real `engine.ts` code against real fixtures (not mocked
internals) — a judgment call that further review rounds against the same
diff had reached diminishing returns, not an assumption that the code is
beyond scrutiny.

### Part 3 — manual verification pass, partial

**Verified live**, against the real deployed `codoro-api-dev` Worker and
the real dev Clerk instance, `pnpm dev` + Vite proxy
(`DEV_API_TARGET=https://codoro-api-dev.codoroapp.workers.dev`), using a
disposable account (`tshore2004+t8bmanualpass@gmail.com`):

- The F31 account-switch path, both the 200 (adopt) and 404 (fresh
  account, push) branches, confirmed by direct IndexedDB inspection (not
  just UI observation) — `anonId` preserved across the switch, `rating`/
  `challengerName`/`attempts` correctly blanked, no cross-account data
  leakage. Run against the code as it stood after round 2's fixes, before
  round 3's three marker-lifecycle refinements landed.
- A clean guest boot on this round's final (round-3-fixed) code: zero
  `/api/*` requests, zero console errors, first paint unblocked — see
  "Part 2," above.

**Not completed, and named here rather than silently dropped**: two-client
same-account sync, a provoked real conflict, airplane-mode play, a clean
anonymous→account migration, both schema-skew directions, and a live
re-run of the account-switch path specifically against round 3's three
fixes. Root cause: this session's own context was compacted mid-pass
(between the switch-path verification above and the rest of the matrix),
and the disposable test account's password did not survive that
compaction — it was never written anywhere durable, only held in the
conversation's working memory. Recovering it would mean either guessing
(rejected — this is exactly the kind of "force a green" this task's own
brief warned against) or creating a fresh disposable account, which risks
a Cloudflare Turnstile challenge with no human at the keyboard to clear it
(this session is a background job; T7b's own closeout, above, already hit
this exact wall on a different origin). Stopped here rather than either.

**What covers the gap instead**: the three round-3 fixes are each covered
by a regression test exercising the real `engine.ts` code — the same
`createSyncEngine`/`handleSignedIn` call chain a live browser pass would
exercise — against real fixtures, using the file's established two-boot
"simulated reload" pattern (`readPendingAdopt`/`readSyncMeta` assertions
after a fresh `createSyncEngine({ getToken })` call standing in for the
post-reload boot). This is process, not a substitute for the live pass —
**the remaining scenario matrix above is still open** and should be the
first thing whoever picks up sync work next actually runs, ideally with a
recorded disposable-account password (a project password manager entry,
not a chat transcript) so a mid-task compaction can't strand it again.

### Final validation

- `tsc -b` (build-mode, project-wide): clean, no errors.
- `pnpm exec eslint .` (whole project, needed a larger heap —
  `--max-old-space-size=8192` — to complete without OOM in this sandbox;
  the default heap OOM'ing is an environment/memory constraint, not a lint
  finding): **3 problems (2 errors, 1 warning), all pre-existing and
  unrelated to this branch's diff** — 2 parsing errors on
  `.claude/worktrees/og-card-challenge/.wrangler/tmp/**` (a different,
  git-untracked worktree's build-cache artifacts, not source), 1
  `react-refresh/only-export-components` warning in
  `src/auth/AuthProvider.tsx` (last touched at Phase 5.1 T5, `#126`, not
  part of this branch). Zero lint issues in any file this branch touches.
- Full suite: 2727/2727 tests green (`pnpm test -- --run`). Coverage run
  separately confirms `src/storage/**` at **100% statements / 96.82%
  branches / 100% functions / 100% lines** — meets the pinned 100/96/100/100
  gate. (Running `vitest --coverage` against the _whole_ suite hit two
  unrelated timeouts in `TraceRunner.od4.pool.test.tsx` — a large
  parameterized loop whose 5s-per-case timeout coverage instrumentation's
  own overhead pushed past; confirmed pre-existing/unrelated by the plain,
  non-coverage full run passing the same two cases cleanly.)
- `pnpm validate`'s single combined invocation OOM'd on its default heap
  during the lint step; not rerun end-to-end with the larger heap flag
  threaded through every sub-command. Each sub-command verified
  individually above instead, which is the substantively same evidence.

### DoD status

Fixed and tested: both given findings, all three round-3 should-fix items,
queue backoff decision recorded, footgun table updated inline (F31 row,
Decision 2, S3). Wired: Part 2, bundle delta recorded. Reviewed: three
rounds, fresh context each time, all findings fixed or explicitly reasoned
through above — none silently dropped. **Not done**: the manual scenario
matrix beyond what's listed under "Verified live" above — an honest gap,
not a forced green, per this task's own brief.

- **Two browser profiles on one machine remain a fully valid stand-in for the merge-engine's own correctness** (per this closeout's own instruction) — that part of T8's DoD doesn't block on solving the phone/Turnstile question at all. Only the literal "two real devices" framing does.

## T8b verification amendment (2026-09-14) — the manual matrix, actually run

Separate session, Sonnet, explicitly scoped as verification-only (no code
changes unless a scenario failed). PR **[#143](https://github.com/codoro-app/codoro/pull/143)**
opened against `main` from `docs/t8b-sync-wiring`, not merged — merge is
Thomas's call. CI (`ci` job, five sub-steps: Typecheck/Lint/Validate
content/Test/Build, plus workers validate, dependency audit, gitleaks) is
**green end-to-end in 5m20s**, each step its own process on GitHub's
runners — this answers the prior session's own open question: the local
`eslint` OOM (needing `--max-old-space-size=8192`) is a sandbox
memory-limit artifact, not a real problem. CI's plain `Lint` step passed
with no extra heap flag.

**Two environment bugs found and fixed before any scenario could run, both
self-inflicted by this session, not app bugs — named because the next
person to run this matrix will hit the same walls if not warned:**

1. **`DEV_API_TARGET` set in `.env` instead of the shell environment.**
   This is F29/T7b's own documented gotcha ("Vite does not inject
   non-`VITE_`-prefixed `.env` values into the config file's own
   `process.env`... it must be a real shell/OS environment variable"),
   re-discovered the hard way: `pnpm dev` silently fell back to the
   `wrangler dev` default target (nothing running there), so `/api/*`
   proxied to a 502 for the entire first stretch of this session,
   presenting as a "stuck pending-adopt marker, engine never pulls" false
   alarm before the real cause (a bad gateway) was isolated via a raw
   `fetch()` from the page console. Fixed by launching
   `DEV_API_TARGET=https://codoro-api-dev.codoroapp.workers.dev pnpm dev`
   (shell-prefixed, not `.env`-sourced).
2. **A stray `vite` process from the same day's earlier T8b coding session
   was still holding port 5173** when this session started (`pnpm dev`
   silently fell back to 5174, which the dev Worker's `APP_ORIGINS`
   allow-list doesn't include). Killed and restarted clean. Windows'
   `TaskStop` on a `pnpm dev` background task does **not** reliably kill
   the underlying detached `node.exe` — this recurred twice more later in
   the session (a "killed" task-notification arrived while the server was
   demonstrably still serving requests) — `netstat`-then-`taskkill` on the
   actual PID was the only reliable stop.

**A third anomaly, isolated but not fully root-caused — most likely another
test-methodology artifact, not a product bug:** after a sequence of raw
`indexedDB.open('codoro')` reads taken directly from the page console
(snapshotting local state for the conflict-scenario setup, see below) while
the SPA stayed mounted (no reload), one real mutation's debounced push
(and an explicit `visibilitychange`-triggered `flush()`) both silently
never fired — `notifyMutation()`'s own unconditional `lastMutatedAt` stamp
still wrote, but no `PUT` ever followed, for 3+ minutes, with Clerk
reporting signed-in the whole time. A **plain page reload immediately
fixed it** — the very next mutation on a fresh engine instance pushed
normally within the debounce window. Since a real user's UI never opens a
second, unmanaged `indexedDB` connection alongside the app's own live one
mid-session, this is flagged as an artifact of this session's own
raw-console probing rather than budgeted further — but it's recorded
here, not silently dropped, in case a future session sees the same
"mutations recorded locally, push never fires, signed-in the whole time"
symptom through ordinary use.

### The six scenarios — all run, all with real evidence

Account: `tshore2004+t8bverify@gmail.com`, created and deleted this session
(type-to-confirm delete, redirected home on success). Thomas created the
account and completed every sign-in step that involved a password field or
Chrome's own credential autofill — Claude does not enter passwords or
create accounts, a standing rule independent of this session's own
authorization. Chrome autofilled a mismatched saved credential
(`tshore_test`, not the real email) into the sign-in modal at one point;
caught before submission and handed back to Thomas rather than risking the
wrong account. Every other step (navigation, puzzle-answering,
network/IndexedDB inspection, raw authenticated `fetch()` calls standing
in for a second device) ran directly.

0. **F31 account switch, round-3 code — CONFIRMED, both branches.**
   Isolation branch happened organically: this browser profile carried
   `codoro:sync-meta` from an unrelated earlier account (a leftover from
   the same day's coding session). Signing up as the new disposable account
   correctly detected the mismatch, wrote the pending-adopt marker, and
   (via this session's own page reload standing in for the app's forced
   reload) resolved boot 2: `GET /api/profile` → **404**, `PUT` → **201**,
   local IndexedDB left genuinely blank (`rating: 1200`, zero attempts,
   `firstRunCompleted: false`) with this device's own `anonId` preserved
   across the reset — zero leakage from the prior account. Merge branch:
   sign out, wipe local storage/IndexedDB, play one real guest puzzle
   (zero `/api/*` calls, confirmed), sign back in — merged cleanly with
   the account's existing server data (`ratedAttemptCount` 1→2, rating
   recomputed to `1227.50`, not maxed or LWW'd; `anonId` took the local
   guest's value per the "local wins" rule).
1. **Two clients, same account — CONFIRMED** (sequential single-tab
   approximation, Thomas's own choice when the Incognito path turned out
   blocked — see below). Played a puzzle on "device A" (rating 1200→1220),
   confirmed the debounced `PUT` landed within ~7s. Wiped local
   storage/IndexedDB (Clerk's own session cookie untouched) to simulate
   "device B," reloaded, confirmed the pulled state matched exactly
   (`rating: 1220.48`, `ratedAttemptCount: 1`).
2. **Provoked conflict — CONFIRMED, real 409 observed and resolved.** A raw
   authenticated `PUT` (this tab's own Clerk token, bypassing the app's UI
   entirely) simulated a second device pushing first at the shared
   `baseRevision`, landing a distinct marker attempt server-side. This
   tab's own next real mutation then produced the actual sequence:
   `PUT` → **409** → `GET` → **200** → `PUT` → **200**, all through the
   real `engine.ts` conflict-retry path, not simulated. Verified past
   "didn't crash": final server state contained **both** the injected
   marker attempt and this tab's own new attempt (`ratedAttemptCount: 7`,
   rating recomputed to `1295.75`) — a real union, not a last-write-wins
   loss.
3. **Airplane mode (I2) — CONFIRMED.** Patched `window.fetch` to reject
   only `/api/*` calls (closer to a real network outage than mocking the
   engine) rather than an OS-level offline toggle — the one deliberate
   substitution in this matrix, since Chrome DevTools' Network-conditions
   "Offline" panel wasn't worth the extra clicks given the fetch-level
   patch exercises the identical failure path the real engine code
   handles. Played a full puzzle while "offline": no spinner, no error, no
   blocked interaction, rating updated locally exactly as a signed-out v3
   session would. The push failed silently and queued
   (`codoro:sync-queue` recorded `attempts: 1`). Restored `fetch` and
   dispatched a real `online` event: queue drained, two `PUT`s landed,
   `codoro:sync-queue` cleared, `baseRevision` advanced.
4. **Anonymous → account migration — CONFIRMED.** Folded into scenario 0's
   merge branch above (a guest playing before sign-in and merging into an
   _existing_ account is a strictly harder case than merging into a brand
   new one, and this session's account already had server data by that
   point) — same evidence: guest attempt merged in, rating recomputed,
   `anonId` correctly took the local (device) value rather than the
   account's previously-linked one.
5. **Both schema-skew directions — CONFIRMED, live, not just unit-tested.**
   Forced via a raw authenticated `PUT` carrying `schemaVersion: 14`
   (remote-ahead) — no local source edit needed, since the skew check
   compares the _pulled blob's_ reported version, not a compiled constant.
   Reload: exactly one `GET`, **zero subsequent `PUT`s even after a real
   mutation** — round 2's fix, confirmed live. Then remote-behind: raw
   `PUT` with `schema_version: 12` (preferences stripped of `sound`/
   `autoAdvance` to make the migration's effect observable). Reload: `GET`
   pulled the v12 blob, `runMigrations()` ran locally (`schema_version`
   restamped to 13, `sound`/`autoAdvance` restored to `true`), and —
   correctly, since `blob < CURRENT` is not read-only — a normal `PUT`
   followed, pushing the migrated state back.

**Named deviation from the original brief, decided live with Thomas:** the
plan called for a second, storage-isolated Chrome profile as the literal
second device for scenarios 1 and 2. `Ctrl+Shift+N` opened an Incognito
window, but Chrome disables extensions in Incognito by default, so the
browser-automation tooling couldn't reach it. Asked Thomas directly; he
chose the sequential single-tab approximation (clear this app's own
storage, keep Clerk's session cookie, reload) over enabling the extension
in Incognito or manually driving a second window himself. This is weaker
than literal concurrency for timing-sensitive races, but every round-trip
was still a real network call against the real deployed `codoro-api-dev`
Worker — the same discipline the T8a handoff note itself blessed ("two
browser profiles on one machine remain a fully valid stand-in for the
merge-engine's own correctness").

### DoD

- [x] PR #143 opened against `main`, not merged. CI green in 5m20s,
      confirming the local lint-OOM is a sandbox artifact.
- [x] All six scenarios run, each with real evidence (status codes, response
      bodies, before/after IndexedDB state) — see above.
- [x] Scenario 5's schema-version skew was forced via raw authenticated
      `PUT` calls carrying a different `schemaVersion`/`schema_version`,
      **not** a local `CURRENT_SCHEMA_VERSION` source edit — nothing to
      revert in the working tree. `git status` confirmed clean of any such
      edit throughout.
- [x] Disposable test account (`tshore2004+t8bverify@gmail.com`) deleted via
      the app's own type-to-confirm flow, redirected home.
- [x] This amendment.
- [x] Production-deploy gap written up below, unexecuted (Part 2).
- [x] `.env`'s two session-local additions (`VITE_CLERK_PUBLISHABLE_KEY`,
      `DEV_API_TARGET`) removed after use; dev server process stopped.

**Net assessment: T8's "verified live" DoD line is now honestly closed.**
Every scenario the T8b closeout amendment named as unrun has real evidence
behind it, gathered against the actual deployed `codoro-api-dev` Worker and
the real dev Clerk instance — not mocked, not only unit-tested. The three
anomalies found along the way were isolated, explained, and (for the two
that were this session's own environment mistakes) fixed before they could
contaminate any scenario's result; the third is named as an open, likely-
benign question rather than quietly ignored. Merge to `main` is still
Thomas's own call.

## Production-deploy gap (found 2026-09-14, written up per this task's own instruction — NOT executed)

Checked against live production the same day this amendment was written:

    GET https://getcodoro.com/api/health  → {"ok":true,"version":"dev","clerkInstance":"production"}
    GET https://getcodoro.com/api/profile → 404 Not Found   (bare Hono 404, not {"error":"Unauthorized"})

Production's `/api/health` response carries no `clerkJwtKeyFingerprint`
field at all (the F30 detector T7b added), and there is no `/api/profile`
route — the production Worker predates both T7 and #141. **Production has
been running a pre-5.2 Worker this entire time**, unrelated to and
unaffected by anything T6/T7/T8a/T8b have built or this verification
session touched (all of it ran against `codoro-api-dev`, never production).

**Why this matters now specifically:** merging `docs/t8b-sync-wiring` to
`main` deploys the _client_ to `getcodoro.com` via Cloudflare Pages, and
the sync engine is now wired into three real mount points
(`App.tsx`/`AccountSection.tsx`/`SignupPromptSheet.tsx`). The moment that
merges, every real signed-in user's browser starts calling
`/api/profile` — against a Worker that doesn't have the route. The
failure mode is exactly I2's "degrade silently" design working _against_
the product here, not for it: pull → 404 → treated as "no profile row
yet" → first-sign-in migration path → `PUT` → 404 (not the `profiles`-
table-missing 500 it would be if D1 migrations were the only gap — a bare
route-404, since the route itself doesn't exist) → `enqueueRetry` → the
retry queue re-fires on every subsequent `online` event, forever, for
every signed-in user, silently. Sync would look shipped in the bundle and
be completely inert in production — worse than a visible error, because
nothing would ever surface it to a user or to Thomas without someone
specifically checking network traffic on production, the same way this
gap was found.

**New task: T9 — deploy the server half of Phase 5.2 to production
(server before client, strictly — sync must never ship to `getcodoro.com`
ahead of the Worker route it depends on).**

Scope, at minimum:

1. **D1 migrations against production**, `--remote`
   (`migrations:apply:production` or whatever the existing package-script
   convention names it — match `deploy:dev`'s naming). A missing
   `profiles` table turns every `PUT` into a `500`, not a `404` — worth
   confirming the table doesn't already exist from some earlier partial
   deploy before assuming a clean apply.
2. **Production Clerk secrets on the Worker**: `CLERK_JWT_KEY` and
   `CLERK_SECRET_KEY` for the _production_ Clerk instance (not the dev
   instance's values — a copy-paste from `.dev.vars` would 401 every real
   user exactly like F30 did in dev, except silently, in production, with
   real users affected instead of a disposable test account). Confirm via
   the same fingerprint mechanism F30's detector already built — see step 5.
3. **`APP_ORIGINS`** on the production environment — already
   `https://getcodoro.com` per the T7b static guard
   (`appOriginsGuard.test.ts`), but confirm the _deployed_ Worker's actual
   value matches (deploys can drift, same caveat T7b's own handoff note
   already flagged for dev).
4. **The deploy itself** — whatever `wrangler deploy --env production` (or
   the existing `deploy:production`-named script) already does for the
   Worker; this task doesn't need to invent new deploy tooling, just run
   the existing dev-env path against the production env for the first
   time.
5. **Post-deploy verification, before calling this done**:
   - `GET https://getcodoro.com/api/health` returns a
     `clerkJwtKeyFingerprint` field (proves the F30 detector is live in
     prod, and gives a standing way to catch a future prod secret drift
     the same way it caught dev's).
   - `GET https://getcodoro.com/api/profile` (unauthenticated) returns a
     **JSON `401`** (`{"error":"Unauthorized"}` or equivalent), not a bare
     Hono `404` — this is the concrete, scriptable signal that the route
     now exists and `clerkAuth()` is wired, without needing a real
     production user's token to check it.
   - One real authenticated round-trip against production, same shape as
     T7b's Piece 3 dev pass (`GET` 404 for a fresh test identity → `PUT`
     201 → `GET` 200) — using a real disposable production account,
     deleted afterward, same hygiene discipline as every other session in
     this plan. This is the step that actually proves sync works
     end-to-end in production, not just that the route responds.

**Ordering constraint, stated plainly because it's the one thing this task
cannot get wrong: T9 (server) must land and be verified _before_
`docs/t8b-sync-wiring` (client) merges to `main`.** If the client merges
first, every real signed-in user hits the dead-route failure mode
described above for however long the gap between merges lasts. Merging
server-then-client, with the post-deploy verification above actually run
in between, is the only ordering that avoids shipping sync as
production-visible-but-silently-broken.

**Not executed this session, per this task's own explicit instruction** —
production `wrangler deploy`/migration apply remain a deliberate manual
step until launch. This section exists so the gap is a named, scoped task
with its own DoD rather than something discovered again from scratch the
next time someone checks `getcodoro.com` directly.

## T9 amendment (2026-09-14) — server deployed to production, two required checks green, one step deferred to a human

Executed as its own manual, confirmed step, per this task's own instruction. Every production-mutating command was named and approved before running.

**Pre-deploy read-only checks** (no production mutation):

- `wrangler d1 migrations list DB --env production --remote` → **"No migrations to apply!"** — confirmed directly via `SELECT name FROM sqlite_master`: `profiles`, `users`, `scores`, `scores_best`, `reports`, `email_prefs` already exist in `codoro-prod`. Step 1 of T9's scope was a no-op; nothing applied this session.
- `wrangler secret list --env production` → `CLERK_JWT_KEY` and `CLERK_SECRET_KEY` already present (names only — from the original T3 production launch, predating this task). Not reset; correctness confirmed instead via the post-deploy fingerprint below, same discipline F30's detector was built for.
- `wrangler deployments list --env production` → last deploy was 2026-09-11, before T7/T7b merged — confirms production had been running a pre-5.2 Worker the entire time this plan document already claimed.
- `git diff main HEAD -- workers/` → empty; `workers/` on `docs/t8b-sync-wiring` is byte-identical to `main`, so deploying from either checkout ships the same code.

**Deploy:** `pnpm run deploy:production` (`workers/`, confirmed by Thomas before running) — succeeded. `Current Version ID: 0c3ef8bb-2be1-4418-8702-8878b2ac7f94`, bound `APP_ORIGINS: "https://getcodoro.com"` as expected.

**Post-deploy verification — the two checks named as this task's required DoD:**

    GET https://getcodoro.com/api/health   → {"ok":true,"version":"dev","clerkInstance":"production","clerkJwtKeyFingerprint":"dfa1dabf"}
    GET https://getcodoro.com/api/profile  → HTTP 401, {"error":"Unauthorized"}  (JSON, not a bare Hono 404)

Both pass. The fingerprint field proves the deployed `CLERK_JWT_KEY` is live and readable via the F30 detector (a standing way to catch future prod secret drift); the JSON 401 proves the route exists and `clerkAuth()` is wired, without needing a real production user's token.

**Deferred — item 5's third bullet, the real disposable-account round-trip:** attempted via browser (`getcodoro.com/settings` → "Sign in or create account"). Chrome autofilled a saved credential into the sign-in modal before any account-creation step was reached. Entering a password (autofilled or typed) to authenticate, or creating an account, are both actions Claude does not perform under any circumstance — not a confirm-first case, a never case — so the session stopped there without submitting the form and closed the tab. This is **not** a gap in the two checks Thomas named as T9's required DoD (both pass, above); it's the plan doc's own extra step (item 5, third bullet) left undone. **Needs a human**: five minutes, same shape as T7b's Piece 3 — sign up a disposable account on `getcodoro.com`, confirm `GET`/`PUT`/`GET /api/profile` round-trips for real, delete the account afterward.

**Ordering constraint satisfied:** T9 (server) is now live and verified in production, ahead of `docs/t8b-sync-wiring` (client) merging to `main` — the ordering this section required is intact. Merging the client branch is Thomas's call, not run as part of this session.

**Update, same day:** `docs/t8b-sync-wiring` (PR #143) merged to `main` at `00886b5`, 2026-09-14 13:30:22 -0400 — after the server deploy above landed and was verified, honoring this section's ordering constraint for real, not just in plan. The client is now live at `getcodoro.com` via Cloudflare Pages, sync engine wired into `App.tsx`/`AccountSection.tsx`/`SignupPromptSheet.tsx`.

## Phase 5.2 closing amendment (2026-09-14) — production round-trip confirmed, phase done

**T9's last open DoD line, now closed.** The T9 amendment above deferred one step to a human: the real disposable-account round-trip against production, because it requires entering a password to sign up, which Claude does not do under any circumstance. Thomas ran it directly in devtools, 2026-09-14:

    signup → GET /api/profile 404 → PUT 201 → play → PUT 200 → reload
    → GET 200 (history intact) → account deleted

This is item 5's third bullet in T9's own scope list, and it's the piece that actually proves sync works end-to-end in production rather than just that the route responds — the two scriptable checks (fingerprint present, JSON 401) prove the route is wired; this proves the whole path (D1 write, single-device round trip, reload rehydration) is real against `codoro-prod`, not just `codoro-dev`.

**Follow-up found, named, scoped — not a blocker:** production's `/api/health` always reports `"version":"dev"`. `workers/src/index.ts:46` reads `c.env.VERSION ?? 'dev'`, and `VERSION` (documented in `workers/src/env.d.ts:56-61`) is only ever set by a `--var VERSION:<sha>` flag at deploy time — nothing in `wrangler.jsonc` sets it, by design (it's meant to vary per-deploy, not be a static env var). CI's `deploy-dev` job already does this correctly (`.github/workflows/ci.yml:109`, `wrangler deploy --env dev --var VERSION:${{ github.sha }}`, run on every push to `main`) — `codoro-api-dev`'s `/api/health` has reported a real commit sha since that job existed. **Production has no equivalent CI job** — production deploys are "a deliberate manual step until launch," the same T2 process note `ci.yml` itself cites for why `deploy-dev` is the only automatic one — and neither today's `pnpm run deploy:production` nor the script it wraps passes `--var VERSION:...`. Net effect: production's `/api/health` can identify which Clerk instance is live (F1's original diagnostic) but not which commit — exactly the first thing you'd want during an incident, and the one piece of the F30-detector-style tooling this phase built that doesn't yet reach production. Scoped fix for whenever production deploys stop being manual (or sooner, cheaply): pass `--var VERSION:$(git rev-parse HEAD)` (or equivalent) the same shape `deploy-dev`'s CI step already does. Not urgent enough to hold this phase closed on.

**Coverage — Phase 5.2's build-plan DoD, against real evidence (not inference):**

| Build-plan DoD bullet                                                                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two-device test; offline-both, reconnect, provably lossless                                            | T8b verification amendment, scenarios 1 (two clients, same account — sequential single-tab approximation, Thomas's own explicit choice after Incognito blocked the browser-automation extension, every round trip still a real network call against the real deployed `codoro-api-dev` Worker) and 2 (provoked conflict — real `409` → `200` → `200`, both attempts present in the merged result, not last-write-wins) |
| Anonymous → account migration keeps rating + history vs. a real pre-v5 export                          | T8b verification amendment scenario 0 (merge branch) + scenario 4, explicitly confirmed; T7b closeout addendum's fixture finding (`CURRENT_SCHEMA_VERSION` has been 13 since v5's first commit — a "pre-v5 export" and a current export are structurally identical right now, so T6's three real fixtures already satisfy F27's actual intent)                                                                         |
| Airplane-mode pass identical to v3                                                                     | T8b verification amendment scenario 3 — `fetch` patched to reject only `/api/*` (closer to a real outage than mocking the engine), full puzzle played with no spinner/error/blocked interaction, push queued silently and drained cleanly on the next `online` event                                                                                                                                                   |
| Schema-version skew defined and tested both directions                                                 | T6's skew policy table (this doc) + T8b verification amendment scenario 5 — both directions forced live via raw authenticated `PUT`s carrying a different `schemaVersion`, not a local source edit; remote-ahead correctly went read-only, remote-behind correctly ran `runMigrations()` then pushed the migrated result back                                                                                          |
| (T9, not a build-plan line but this phase's own late addition) Server deployed to production, verified | T9 amendment (above) — both required checks pass; production round-trip closed just above                                                                                                                                                                                                                                                                                                                              |

`docs/v5-build-plan.md`'s Phase 5.2 DoD checkboxes updated against this table, same convention #124 used for 5.0 and the Phase 5.1 closing amendment used for 5.1.

**Net assessment: Phase 5.2 is done.** Merge engine (T6), server endpoints (T7/T7b), client orchestration (T8/T8a/T8b), and the production deploy (T9) are all live, each with real evidence — fixture-based property tests plus a real deployed-Worker pass for T6/T7, a six-scenario real click-through matrix against `codoro-dev` for T8, and now a real signup-through-delete round trip against `codoro-prod` for T9. The one deliberately-loose thread (literal two-separate-browser-profile concurrency, as opposed to the sequential single-tab approximation actually run) is named above rather than silently assumed away, matching this plan's own Verification discipline section. The `/api/health` version-fingerprint gap is the only new finding this amendment adds, and it's scoped, not blocking.
