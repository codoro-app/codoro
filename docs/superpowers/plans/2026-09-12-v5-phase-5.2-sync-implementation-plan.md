# v5 Phase 5.2 — sync: detailed implementation plan (2026-09-12)

Companion to `docs/superpowers/plans/2026-08-27-v5-accounts-implementation-plan.md` (T6/T7/T8 — this is that plan's Phase 5.2 section expanded to task-by-task detail, in the same convention as the 5.0 coding plan and the 5.1 build prompt) and `docs/v5-build-plan.md`'s Phase 5.2 section (the DoDs this plan is scoped to satisfy).

**Read first, every session:** I1–I10 in the accounts implementation plan, the Footgun Register (F1–F23), and this document's own footgun additions below (F24–F28). A task that violates an invariant is wrong even if its tests pass.

## Phase 5.1 status — verified before writing this plan (not assumed)

Checked against the repo directly (git log, source tree, workers routes) rather than taken from the plan doc's own claims:

- **T5 merged to `main`** (#126), then hardened by seven real-world fix PRs (#127–#133) once real Clerk keys existed and Thomas ran the actual flow against production via Claude in Chrome: a dead-CSS-preload crash on live `/settings` (#128, critical), missing captcha mount point (#129), password minLength mismatch with Clerk's production policy (#130), a verification email that was never actually being sent (#131), a stale Worker route blocking `deploy-dev` CI (#132), and username collection + real Clerk error surfacing (#133, `missing_requirements` deadlock). One more commit (`sign-up's username also fills in the challenge name`) is pushed to `feat/signup-sets-challenger-name` but **not yet merged to `main`** — small (29 lines, one file), not a blocker for 5.2 planning, worth landing first so 5.2 starts from a clean `main`.
- **DoD status, from the T5 session amendment plus the fix-PR trail:**
  - Signed-out play loop unchanged, bundle diff + Lighthouse recorded — **done**.
  - Create → sign out → sign in → delete round-trip — server-side deletion (D1 row gone, idempotent, cross-user isolated) was verified against local bindings in T5 itself; the _client_ round-trip is what #127–#133 exist to make actually work end-to-end against Clerk Production (email verification literally didn't fire before #131) — **functionally done now**, but no single amendment explicitly re-states "the full round-trip was clicked through end-to-end post-fixes." Worth a five-minute manual pass before calling 5.1 fully closed, not a reason to hold 5.2.
  - Signup prompts: frequency-cap logic is unit-tested (15 tests) and one of four triggers is wired end-to-end (`stats-second-visit`). **`boss-clear` and `streak-7-day` are built (copy + config) but not wired to real UI** — a named, scoped gap in the T5 amendment, not an oversight discovered now. `leaderboard-view` structurally can't be wired before 5.3 (no leaderboard page exists).
  - Report control: signed-out, fire-and-forget, server round-trip tested; a manual click-through against `codoro-dev` was noted as "not yet performed" in the T5 amendment — likely covered incidentally by the live debugging in #128 (which used `getcodoro.com/settings` and Claude in Chrome), not confirmed explicitly.
  - `docs/v5-build-plan.md`'s own Phase 5.1 DoD checklist is still all `- [ ]` — nobody has written the 5.1 closing amendment (the same housekeeping step #124 did for 5.0). **Recommend doing that now**, before or alongside 5.2's Piece 0, so the record matches reality.
- **Net assessment: Phase 5.1 is substantively complete.** The core client-auth surface (sign-up, sign-in, delete, settings) is live in production and has been debugged against real usage, which is a stronger bar than the original DoD's staging-only ask. The two real gaps — two of four signup-prompt triggers unwired, and no written 5.1 closing amendment — are small, named, and don't block 5.2 (sync doesn't depend on either). Recommend closing them in Piece 0 below rather than letting them sit as an ambiguous "done or not" question.

## What already exists that 5.2 builds on (verified in the repo, not assumed from the plan)

- **D1 schema is already fully built** — migration `0001_init.sql` (landed in Phase 5.0, T2/T2a) already contains `profiles` (compressed `BLOB payload` + `payload_bytes`, per the 2026-08-31 amendment's S1) with `revision`, `schema_version`, `updated_at`. `workers/src/profileStore.ts` (T2a's boundary) already exists with the get/put/delete surface the amendment describes, gzip in the Worker via `CompressionStream`/`DecompressionStream`. **T7 does not create this table or the compression boundary — both are done.** T7 is: write the two route handlers, the optimistic-concurrency check, and the payload-cap validation on top of `profileStore.ts`.
- **`scores`/`scores_best` tables also already exist** (S3, plus migration `0002` for an ops index) — built ahead of schedule in Phase 5.0 rather than in T10/Phase 5.3 as the original plan sequenced it. Not this phase's problem, but worth knowing so nobody re-derives the DDL.
- **`workers/src/index.ts` currently registers exactly three routes**: `GET /api/health`, `POST /api/report`, `DELETE /api/account`. No `/api/profile`, no `/api/scores`, no `/api/leaderboard`, no `/api/username`, no `/api/privacy` — **confirms 5.2 has not been started**.
- **`src/auth/api.ts`** is the one fetch wrapper (T5) — `apiFetch(path, { method, token, body, timeoutMs })`, throws typed `ApiError` with a `kind` taxonomy (`network`/`timeout`/`unauthorized`/`forbidden`/`not-found`/`rate-limited`/`server`/`invalid-response`/`client`). **T8's sync engine calls this, doesn't reinvent it.**
- **`src/storage/`** is the existing local-first layer T6/T8 must not couple to structurally: `schema.ts` (`UserProfile` interface, `CURRENT_SCHEMA_VERSION = 13`), `profile.ts` (`loadProfile()`/`saveProfile(profile)` — the single write boundary, called directly by feature code like `BossPage.tsx`, no pub/sub layer exists), `attempts.ts` (`appendAttempt()`/`listAttempts()` — attempts are a **separate IndexedDB store**, not a field on `UserProfile`), `migrations.ts` (`MIGRATIONS`/`runMigrations()` — the forward-only chain T6's `blob < CURRENT` branch reuses), `exportImport.ts` (`ExportedData = { schema_version, exportedAt, profile: UserProfile, attempts: Attempt[] }` — the wire shape sync pushes/pulls; note `importData()` is a **wholesale replace** of both stores, not a merge — T6 is new logic, not an extension of anything here).
- **No pub/sub/listener layer exists in `src/storage/`** (verified — no `addEventListener`/`subscribe`/`onChange` pattern). The plan's "flows through the normal storage listeners" line doesn't correspond to a real mechanism yet in this codebase; T8 has to decide how push-after-boundary actually triggers (see the open design question in T8 below — this is new, not a wiring exercise).
- **`fast-check` is already a devDependency** (`^4.9.0`) — T6's property tests use it directly, no new dependency to add.
- **No real pre-v5 export fixture exists in the repo yet** (`find . -iname "*fixture*"` — nothing). T8's DoD explicitly requires replaying migration against "a real pre-v5 export file," not a synthetic one — getting one is a Piece 0 prep step, not a T8-time scramble.
- **`UserProfile`'s actual fields** (for the merge-rule table, from `src/storage/schema.ts`): `schema_version, rating, ratedAttemptCount, streak, requeueState, storagePersisted, dailyCompletion, rushStats, bestRunStreak, bossStats, missionProgress, missionStats, preferences, anonId, challengerName, firstRunCompleted`. Plus `attempts: Attempt[]` as a sibling in `ExportedData`, not a `UserProfile` field. This is the exhaustive list T6's `Record<keyof Profile, MergeRule>` must cover.

## Piece 0 — close out 5.1's loose ends (small, do first)

Not because 5.2 depends on it technically, but because starting Phase 5.2 while Phase 5.1's own record is ambiguous is how "is 5.1 done?" becomes a recurring question instead of a closed one.

1. Merge `feat/signup-sets-challenger-name` (or finish whatever review it's waiting on).
2. Manual pass: create → sign out → sign in → delete, end to end against `getcodoro.com`, post-#133. Takes five minutes: it's the one 5.1 DoD line with no explicit "verified post-fix" evidence.
3. Write the Phase 5.1 closing amendment at the bottom of the implementation plan doc, same convention as T1–T4a's and T5's own: what shipped, the fix-PR trail as the real-world verification evidence, `boss-clear`/`streak-7-day` named as deliberately-deferred scoped follow-up (not silently dropped), report-control click-through status. Check off `docs/v5-build-plan.md`'s Phase 5.1 DoD boxes against real evidence, same as #124 did for 5.0.
4. Decide, explicitly, whether `boss-clear`/`streak-7-day` wiring happens now (cheap — `SignupPromptTrigger` is already generic, per the T5 amendment) or is deliberately deferred past 5.2. Either is fine; leaving it undecided isn't.

None of this blocks starting T6 in parallel if you'd rather — T6 is pure logic with zero dependency on 5.1's UI surface — but it should land before 5.2's own closing amendment gets written, so that one doesn't inherit 5.1's unfinished bookkeeping.

## New invariants for this phase

| #   | Invariant                                                                                                                                                                                                                                  | Concrete test                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I11 | **Merge never runs twice for the same pulled revision.** Applying the same server blob to the same local state a second time (a retried pull, a duplicate event) must be a no-op, not a double-union or a re-recompute drift               | `merge(merge(local, remote), remote) === merge(local, remote)` — idempotence test alongside T6's existing commutativity/associativity-style property tests |
| I12 | **A push never races a pull for the same local write.** The engine has exactly one in-flight direction at a time per boundary; a pull arriving mid-push (or vice versa) queues, it doesn't interleave against a half-written `UserProfile` | Concurrency test: trigger pull and push in the same tick, assert `saveProfile` is called with a fully-merged, never a partial, state                       |

These sit alongside I1–I10 unchanged — I2 (local-first lock) and I8 (SW never caches `/api/*`) are the two that bite hardest in this phase specifically, since T8 is the first task that makes real network calls on every normal play session rather than only at auth boundaries.

## T6 — merge engine, pure and alone (1 session — no I/O, no network)

**Files:** `src/sync/merge.ts`, `src/sync/merge.test.ts`, `src/sync/fixtures/` (real export files, see Piece 0-adjacent prep below).

Per-field merge rule, against the actual `UserProfile` shape verified above (write this table into the module's own doc comment, exhaustive via `Record<keyof UserProfile, MergeRule>` so a new profile field with no rule fails typecheck, not silently falls through):

| Field                                                                                                         | Rule                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `schema_version`                                                                                              | not merged directly — governs which branch of the skew table (below) runs; post-merge the result is stamped with the client's own `CURRENT_SCHEMA_VERSION`                                                                                           |
| `rating`                                                                                                      | **recompute**: re-derive from the merged `attempts` array via the same rating engine the client already uses for a fresh attempt, not a max/LWW pick between the two raw numbers                                                                     |
| `ratedAttemptCount`                                                                                           | **recompute**, alongside `rating` — same source, same pass                                                                                                                                                                                           |
| `streak`                                                                                                      | **recompute** from merged attempts' `localDateString`s, per the existing streak-computation logic — not max, not LWW (a stale streak can't just be the bigger number; it has to be re-derived from the actual merged activity)                       |
| `requeueState`                                                                                                | **latest `clientUpdatedAt` wins** — it's session-local scheduling state, not accumulated history                                                                                                                                                     |
| `storagePersisted`                                                                                            | **latest `clientUpdatedAt` wins** — a per-device capability flag, not something to union                                                                                                                                                             |
| `dailyCompletion`                                                                                             | **latest by date, then latest `clientUpdatedAt`** — date-scoped per its own doc comment in `schema.ts`; a stale date from either side means "not completed today," so compare dates first, only fall back to recency on a genuine same-day tie       |
| `rushStats`                                                                                                   | **non-null wins over null; else max/latest per sub-field** per the original plan's rule (bests = max, `lastRunAt` = max, counts = max — stated trade-off: counts are monotonic per device but not summable across devices without double-count risk) |
| `bestRunStreak`                                                                                               | **max**                                                                                                                                                                                                                                              |
| `bossStats`                                                                                                   | same shape as `rushStats` — non-null wins over null, else max/latest per sub-field                                                                                                                                                                   |
| `missionProgress`                                                                                             | **latest `clientUpdatedAt` wins**, non-null wins over null — an in-progress run is inherently single-device state; merging two in-progress runs field-by-field would produce a run that never happened on either device                              |
| `missionStats`                                                                                                | same shape as `rushStats`/`bossStats` — max/latest per sub-field                                                                                                                                                                                     |
| `preferences`                                                                                                 | **latest `clientUpdatedAt` wins** — true settings, last-write-wins per the original plan                                                                                                                                                             |
| `anonId`                                                                                                      | **keep local** — never overwritten by a pulled value (matches T7's server-side "first-write-wins, written once" rule; the client-side mirror of that same invariant)                                                                                 |
| `challengerName`                                                                                              | **latest `clientUpdatedAt` wins**                                                                                                                                                                                                                    |
| `firstRunCompleted`                                                                                           | **OR** (true wins) — once true on any device, it should read true everywhere; there's no legitimate path back to false                                                                                                                               |
| `attempts` (sibling of `UserProfile` in `ExportedData`, not a profile field, but part of the same merge pass) | **union by `id`**, then re-sort by `createdAt`; never drop — the union feeds the `rating`/`ratedAttemptCount`/`streak` recompute above, so this merge has to run before those, not after or in parallel                                              |
| unknown fields (older client, newer payload)                                                                  | **preserved verbatim, round-tripped** — F9; the exhaustive `Record` type only covers fields this client's `schema.ts` knows about _today_; anything else on the wire passes through untouched                                                        |

**Schema-skew policy** (unchanged from the original plan, restated because it's the part most likely to get "simplified" under deadline pressure):

| Pulled blob vs. local `CURRENT_SCHEMA_VERSION` | Action                                                                                                                                                                                                    |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `blob < CURRENT`                               | Run `src/storage/migrations.ts`'s existing `runMigrations()`/`MIGRATIONS` chain over the blob first — the same path an old IndexedDB read or an old import file already takes. No sync-specific migrator. |
| `blob === CURRENT`                             | Merge normally, per the table above.                                                                                                                                                                      |
| `blob > CURRENT`                               | **Read-only sync**: do not merge, do not push. Surface a "reload to update" notice (T8's job to wire; T6 just needs to return a distinguishable result for this branch).                                  |

**Property tests** (`fast-check`, already a devDependency): `merge(a, a) = a`; `merge(a, b) = merge(b, a)` for the union/max fields; merge never decreases an attempts count or a rating-affecting best; **new — I11's idempotence**: `merge(merge(a, b), b) = merge(a, b)`. Fixtures: one fresh-install profile, one long-lived profile, one post-mission profile — **these need to be real exports, not hand-typed objects** (get them from Thomas's own dev profile via the existing `exportData()` / Settings export button, or a throwaway test account played through a mission — either way, before this task starts, not improvised mid-task).

**DoD:** exhaustive `Record<keyof UserProfile, MergeRule>` compiles; all three skew branches tested in both directions; unknown-key round-trip test; I11 idempotence test; zero imports from `src/storage`'s IndexedDB layer (`db.ts`), `fetch`, or `@clerk/react` in `merge.ts` — it takes two `ExportedData`-shaped objects in, returns one out, nothing else.

## T7 — sync endpoints, server half (1 session)

**Files:** `workers/src/routes/profile.ts` (or inline in `index.ts` if the file stays small — match the existing convention, `report.ts` is the closest precedent), `workers/test/profile.test.ts`. No migration needed — the `profiles` table and `profileStore.ts` already exist.

`PUT /api/profile` — body `{ schemaVersion, payload, clientUpdatedAt, baseRevision, anonId? }`. Optimistic concurrency: `UPDATE profiles SET revision = revision + 1, ... WHERE clerk_user_id = ? AND revision = ?` (conditional on `baseRevision`), check `meta.changes === 1` (F10 — a statement that matches zero rows is not success), else `409` with the current server state (fetched via `profileStore.get()`). First `PUT` for a user creates the row (`revision = 1`). Payload cap: **256 KB on the decompressed JSON**, checked before `profileStore.put()` does its internal compression (per the 2026-08-31 amendment — F11's cap and S1's compression are two different layers, don't conflate them). `anonId`: written only if the column is currently null — first non-null value wins, never overwritten after.

`GET /api/profile` — `profileStore.get(userId)`, `404` if no row yet, else `{ revision, schemaVersion, payload, updatedAt }`.

Row creation for `users`: already handled by T3's lazy-insert-on-first-authenticated-write — T7 doesn't duplicate that logic, just calls into the existing `clerkAuth()` middleware and trusts `c.get('userId')` the same way `account.ts`'s `DELETE` handler already does.

**DoD:** conflict path tested (two writers racing from the same `baseRevision` — exactly one 200, one 409 with current state); a real 257 KB payload test (rejected) and a 256 KB one (accepted); authz fixture reused from T3/`account.test.ts` (user A's token can't read or write user B's profile row — this is a `WHERE clerk_user_id = ?` away from being automatic, but write the test anyway, I5 doesn't accept "obviously correct"); `anonId` first-write-wins tested (second `PUT` with a different `anonId` doesn't change the stored one); `404` on `GET` for a user with no profile row yet.

## T8 — sync orchestration, client half (1–2 sessions — the one with a real open design question)

**Files:** `src/sync/engine.ts`, `src/sync/queue.ts`, tests.

**Open design question, decided here rather than discovered mid-task:** the plan's original T8 language ("flows through the normal storage listeners") assumes a pub/sub layer over `src/storage/` that **does not exist** — `saveProfile()` is called directly by feature code (`BossPage.tsx` and siblings) with no event emitted. Two real options:

1. **Wrap `saveProfile`**: add an optional module-level subscriber list inside `src/storage/profile.ts` itself (`onProfileSaved(cb)`), fired at the end of `saveProfile()`. Minimal surface change to a locked, well-tested file — but it does mean touching `profile.ts`, which the 5.1-era convention (see `exportImport.ts`'s own doc comment: "do not modify the storage functions... if you find yourself wanting to, that's a finding to report") treats as something to be careful about, not forbidden.
2. **Call sites opt in**: every profile-mutating boundary (attempt recorded, run ended, settings changed) also calls `syncEngine.notifyMutation()` explicitly, right next to its `saveProfile()` call. No change to `src/storage/`, but it's an easy-to-miss convention — a new boundary that calls `saveProfile()` and forgets the sync hook silently drifts out of sync (no test catches a _missing_ call the way it'd catch a wrong one).

**Recommendation: option 1**, with a single grep-able test (same shape as F13's SW-denylist assertion and T2a's `payload` guard) asserting every call site that saves a profile-mutating boundary also triggers a notify — cheapest to keep honest going forward, and it's one function's internals, not new IndexedDB surface. Settle this explicitly before writing `engine.ts`; it's the one place this task can quietly turn into a game of whack-a-mole across every feature page that calls `saveProfile`.

**Behavior:**

- **Pull on boot** when signed in — non-blocking (I2): app renders from IndexedDB immediately, the pull's merge result (if any) flows through the same save path as any other local mutation.
- **Push**, debounced ~5s, after the mutation-notify hook above fires.
- **Retry queue** (`src/sync/queue.ts`): exponential backoff, capped, persisted across reload (a small IndexedDB store or `localStorage` — pick one, record why; the queue itself is disposable UI-adjacent state, not app data, so `localStorage` is defensible per the same tier `useSignupPrompt.ts` already uses), drained on the `online` event.
- **Every failure path degrades to exactly-v3 behavior silently** (I2) — a failed push doesn't block play, doesn't surface an error to the user mid-puzzle, just requeues.
- **First-sign-in migration is the ordinary flow, not a special case**: local profile + server `404` → push as revision 1; server profile + fresh device → pull then merge (T6's laws handle an empty local profile merging cleanly — verify this is actually true for the _current_ `createDefaultProfile()` shape, not assumed from the original plan's confidence). Link `anonId` on that same first push.
- **Telemetry**: `sync_push`/`sync_pull`/`sync_conflict` counts only — no payload contents (I4).

**DoD:** airplane-mode pass (I2 — signed in, offline, behaves exactly like v3); reload-with-pending-queue test; **I12** (concurrency: pull and push in the same tick don't interleave against a partial state); two real devices against the dev env, recorded in this task's own amendment; a `sync_conflict` actually provoked and observed resolving (not just unit-tested); **anonymous → account migration replayed against a real pre-v5 export file** (get one before this task starts — see the fixtures note under T6); both schema-skew branches exercised end-to-end against the dev env. **See "Verification discipline" below — the two-real-devices pass is a real manual click-through, not a scenario with an equivalent passing test.**

## Coverage — Phase 5.2's build-plan DoD lines

| Build-plan DoD bullet                                                         | Task                         |
| ----------------------------------------------------------------------------- | ---------------------------- |
| Two-device test; offline-both, reconnect, provably lossless                   | T8                           |
| Anonymous → account migration keeps rating + history vs. a real pre-v5 export | T8                           |
| Airplane-mode pass identical to v3                                            | T8 (I2)                      |
| Schema-version skew defined and tested both directions                        | T6 (policy), T8 (end-to-end) |

## Footgun additions for this phase

| #   | Footgun                                                                                                   | The mistake                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Prevented by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F24 | **Recomputing `rating`/`streak` from a partially-merged `attempts` array**                                | T6's field table has `attempts` uniting first, then `rating`/`ratedAttemptCount`/`streak` recomputing from the _result_. Doing them in field-declaration order instead of this dependency order produces a rating computed from only one device's history                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The merge function's own internal ordering is asserted by a test: recompute a rating from a two-device union and confirm it differs from (and is correct relative to) either single-device recompute                                                                                                                                                                                                                                                                                                                                                   |
| F25 | **A missing `notifyMutation()` call at a new boundary**                                                   | Every new feature page that calls `saveProfile()` directly is a place sync can silently stop happening for that boundary, with no failing test — precisely the risk the "option 1 vs. option 2" design question above exists to close off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Wrapping `saveProfile()` itself (option 1) removes the possibility structurally; if option 2 is chosen instead, a grep-based static test enumerating known `saveProfile()` call sites against known `notifyMutation()` call sites is not optional                                                                                                                                                                                                                                                                                                      |
| F26 | **Trusting `createDefaultProfile()`'s current shape to merge cleanly against real data without checking** | The original plan asserts "empty local merges cleanly by T6's laws" as a given. It's true only if every field's merge rule actually degrades correctly against `createDefaultProfile()`'s specific values (e.g. is `rating` in a fresh profile `0`, or some other sentinel that a max/recompute rule handles wrong?)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | T6's fixture set explicitly includes a fresh-install export (not a hand-typed stand-in for one), and the property tests run against it same as the other two fixtures                                                                                                                                                                                                                                                                                                                                                                                  |
| F27 | **Testing the merge engine only against synthetic fixtures**                                              | Hand-typed test objects can't be wrong about production data's actual shape — they encode the author's assumptions about that shape, which is exactly the risk `anonId`/`challengerName`/nullable-field edge cases live in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | T6 and T8 both require _real_ exported fixtures (T6's DoD names it, T8's DoD requires a real pre-v5 export specifically) — get them before either task starts, not during                                                                                                                                                                                                                                                                                                                                                                              |
| F28 | **A retry queue that survives reload but not a schema bump**                                              | A push queued before a client update, replayed after, could carry a `schemaVersion` the _server_ has never seen from this path if the queue itself isn't schema-version-aware                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Queue entries store the `schemaVersion` they were built against; the drain path re-checks it against current `CURRENT_SCHEMA_VERSION` before replaying, not just before building                                                                                                                                                                                                                                                                                                                                                                       |
| F29 | **`codoro-dev`'s authenticated routes are unreachable by any real browser flow today**                    | Found doing T7's own "curl a real token against `codoro-dev`" DoD step: `clerkAuth()` requires the token's `azp` claim to match `APP_ORIGIN`, hardcoded to `"https://getcodoro.com"` in both wrangler.jsonc envs. A backend-minted real Clerk token (`sessions.createSession`/`getToken`, Clerk's own documented testing primitive) carries no `azp` at all and 401s correctly — that's `clerkAuth()` working as designed, not a bug. But no real browser session can satisfy the check either: nothing hosted at `https://getcodoro.com` talks to the _development_ Clerk instance (the real frontend there is wired to _production_ Clerk + the production Worker route; dev has no route of its own, per this same file's 2026-09-11 wrangler.jsonc amendment, and no separate dev frontend origin exists). A token with `azp=https://getcodoro.com` can only come from the production Clerk instance, which dev's `CLERK_JWT_KEY` (the _development_ instance's public key) can never verify. The two requirements are mutually exclusive as currently wired — this is a pre-existing gap in dev's env config, not something T7 introduced, and it silently blocks **T8's own DoD**, which names "two real devices against the dev env" as a required manual pass | Not fixed here (scope decision, not an oversight) — flagging before T8 starts rather than after, since T8's DoD assumes this path works. Resolving it is its own small task: decide what dev's `APP_ORIGIN` should actually be (a real dev frontend origin, once one exists) so a genuine end-to-end pass against `codoro-dev` becomes possible, or scope T8's two-device DoD to run against `getcodoro.com` (production Clerk, production Worker) instead, on a real but disposable account, the same discipline 5.1's own click-through already used |

## Verification discipline — a real click-through, not just green tests

**Why this is its own section, not just a DoD bullet:** 5.1's own history is the argument. T5 shipped with `pnpm validate` green and 2616 passing tests, its DoD boxes checked from local-bindings evidence — and then needed seven follow-up fixes (#127–#133) once Thomas actually clicked through sign-up on production, including one _critical_ crash on live `/settings` and a verification email that had never once actually sent. None of that was a testing failure in the narrow sense — the unit and integration tests that existed were accurate about what they covered. The gap was that nothing in the loop exercised the real, wired-together path (real Clerk instance, real browser, real network) before calling the task done. Automated coverage and "someone actually clicked through it" caught different bugs; 5.1 only had the first until after merge.

5.2 has more surface area for this exact failure mode than 5.1 did, not less: T8 is the first task where the client talks to the Worker on every normal play session, not just at auth boundaries, and merge bugs are the kind that hide behind passing unit tests built from the merge author's own assumptions (F27, above) right up until two real devices produce a real conflict.

**Concretely, add a manual click-through pass to each task's own DoD, distinct from and in addition to its automated tests:**

- **T6**: no click-through of its own (it's pure logic, no I/O) — but its fixtures must be real exports (already required by its DoD and F27), which is this task's version of the same discipline: real data, not the implementer's mental model of real data.
- **T7**: attempted 2026-09-12, immediately after merge — deployed `codoro-dev` was confirmed live (`/api/health` over real network), and a real Clerk-signed token was minted against the development instance via the Backend API (`sessions.createSession`/`getToken`, Clerk's own documented mechanism for exactly this kind of test) and sent as a real `Authorization: Bearer` header to the real deployed Worker. It 401'd — correctly, per **F29** below: the token has no `azp` claim, and `clerkAuth()`'s `authorizedParties` check requires one. Digging into _why_ surfaced F29 itself, a real pre-existing gap in how `codoro-dev` is wired, not a T7 bug: no real browser flow can currently produce a token that satisfies this check against dev at all. This still exercised the real network path, the real deployed code, and real Clerk signature verification (not a locally-forged test JWT) — it just couldn't reach a positive-path (200/201) result given F29. Not pursued further this session (reverse-engineering Clerk's Frontend API ticket-redemption flow to fabricate a matching `azp` was judged not worth the risk/cost of several more blind attempts) — logged as F29 instead of forced to a false green.
- **T8**: this is the one that matters most. Before signing off T8's DoD, do the two-real-devices pass _as an actual manual session_ — two browsers (or a phone + a laptop), signed into the same account, played offline-and-reconnected on purpose, watched for a real `sync_conflict` to resolve. **F29 changes where this has to run**: `codoro-dev` cannot host this pass today (no real browser session can authenticate against it) — until F29 is resolved, this needs to run against `getcodoro.com` (production Clerk, production Worker) on a real but disposable account, the same discipline 5.1's own click-through already used. The DoD already lists this as a requirement; this note is making explicit that it has to be a genuine click-through with eyes on the result, not a box checked because the equivalent scenario has a passing test. Screenshot or otherwise record what was seen, same convention as the amendments throughout this plan and its predecessor.
- **Phase close-out**: before writing 5.2's closing amendment, do one more full pass of the actual user-facing flow this phase adds — sign in on a fresh device with existing play history, confirm it shows up, go offline, play, reconnect, confirm nothing was lost — the same shape of pass that would have caught #128–#131 in 5.1 before merge instead of after.

This isn't a call for more tests; T6/T7/T8's automated DoDs above are already thorough. It's a standing rule that a phase isn't done until someone has actually used the thing it built, once, for real — separate from and after the automated suite is green.

## Suggested sequencing

T6 first, alone — it's pure logic, has zero dependency on the server or on 5.1's remaining loose ends, and every other task in this phase depends on its merge-rule table being settled. T7 next (small, mechanical given `profileStore.ts` already exists). T8 last, and budget it at the high end of 1–2 sessions — it's the task with a real open design question (the `saveProfile` hook point), the one requiring two real devices against the dev env, and the one requiring a real pre-v5 export fixture that doesn't exist yet. Piece 0 (closing out 5.1) can run in parallel with T6, or immediately before it — either way, before T8's DoD is signed off, since T8's own amendment is where "is 5.1 actually done" stops being an open question by simply not needing to be one anymore.
